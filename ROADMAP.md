# Roadmap

## 已完成

- `v0.1.0-dev.1`：初始化规划文档、协作规范、开发记录和项目方向。
- `v0.2.0-dev.1`：完成 Electron 桌面壳、本地 Codex / Git 采集、总览页、账本页、代码仓库页和挂件原型。
- `v0.2.2-dev.*`：持续完成总览页、设置页、刷新历史、设计 token、UI Contract、增量缓存和仓库页视觉收敛。
- `v0.3.0-dev.1` 至 `v0.3.0-dev.9`：升级账本页趋势图、放大查看、额度重置确认规则和周额度分桶口径。
- `v0.3.0-dev.10`：面向公开开源补齐 README、公开完善规划、仓库根目录设置、CI、Windows 打包 workflow、PR 模板、安全策略和贡献说明。
- `v0.3.0`：private 仓库内部正式版，用于内测验证 Windows 安装包、便携包、设置页仓库根目录配置和主要数据链路。
- `v0.3.1-dev.2`：补齐换电脑后的数据接线方案，设置页支持 Codex 数据目录配置和恢复默认，后续 Windows 打包只保留安装版。
- `v0.3.1-dev.3`：新增首次加载自动检测流程，默认路径检测中显示进度说明，检测失败后引导手动选择 Codex / Git 数据源。
- `v0.3.1-dev.4` 至 `v0.3.1-dev.10`：接入共享 `codex-usage-core`，统一额度 reset 规则，纠正“充值 / 重置”命名，并在总览页展示赠送重置次数和逐条过期提醒。
- `v0.3.1`：private 仓库内部正式版，用于内测验证首次数据源接线、共享核心包依赖和赠送重置次数展示。
- `v0.3.2-dev.1`：升级 `codex-usage-core v0.1.0-dev.6`，用公开发放事件 seed 推断首次已有赠送重置的获取时间、预计过期时间和保守提醒时间。
- `v0.3.2-dev.2`：升级 `codex-usage-core v0.1.0-dev.7`，不再默认把 `2026-06-11` launch free reset 归因到当前库存，避免已使用的早期 reset 误显示为可用。
- `v0.3.2`：private 仓库内部正式版，用于内测验证赠送重置次数的公开发放估算、观测新增识别和保守过期提醒。
- `v0.3.3-dev.1`：新增本机系统通知，按 live 快照提醒赠送重置到期 / 无法反推和 5H / 周额度低余量，并用本机 `notification-state.json` 去重。
- `v0.3.3`：private 仓库内部正式版，用于内测验证本机系统通知、通知去重和当前三页主流程。
- `v0.3.4-dev.1`：补齐应用内提醒中心和通知详情，支持未读数、详情弹层、查看页面和标记已读；托盘图标改为运行时 PNG，避免 Windows 托盘空白。
- `v0.3.4`：private 仓库内部正式版，用于内测验证应用内提醒中心、通知详情、已读状态和 Windows 托盘图标修复。
- `v0.3.5-dev.1`：设计通知频率策略，新增通知页筛选分页和详情侧栏；重排设置页，补齐通知策略、Git 本机状态、刷新历史摘要和品牌一致的 Windows 托盘图标。
- `v0.3.5-dev.2`：将通知频率从冷却重复提醒改为固定里程碑一次性提醒；赠送重置按过期前 `7d / 3d / 1d / 12h / 1h` 各提醒一次，额度按周期阈值到达时提醒一次。
- `v0.3.5`：private 仓库内部正式版，用于内测验证通知页、一次性通知里程碑、设置页重排、Git 本机状态和 Windows 托盘品牌图标。
- `v0.3.6-dev.1`：修正 Windows 应用图标资产和打包配置；设置页回到单列卡片流，完整刷新历史改为独立分页页面。
- `v0.3.6-dev.2`：升级 `codex-usage-core v0.1.0-dev.8`；移除 `2026-06-11` banked reset 强制匹配；首次未知 credit 按 `2026-06-14` 假定，第三次 credit 沿用 `2026-07-01T19:58:24.705Z` 本地观测新增，并用 active baseline 防止滚动历史裁剪后丢失。
- `v0.3.6-dev.3`：优化赠送重置展示文案；总览普通态明确显示“最早预计过期”和“建议使用时间”，展开详情主标题直接标注“预计过期”，避免把建议时间误读为过期时间。
- `v0.3.6`：private 仓库内部正式版，用于内测验证 Windows 品牌图标、赠送重置三次归因、设置页单列结构、刷新历史独立页面和赠送重置过期时间文案。
- `v0.3.7-dev.1`：修复刷新历史页缺少返回入口；移除设置页通知策略卡；Git 与授权区明确当前不检测 GitHub 登录，后续接入 GitHub 云端能力时再提供授权引导。
- `v0.3.7-dev.2`：补齐新用户使用路径和 Git 安装指引；本机未安装 Git 时明确 Codex 用量功能仍可用，代码仓库统计降级，并在设置页和代码仓库页提供 Git for Windows 下载入口。
- `v0.3.7`：private 仓库内部正式版，用于内测验证刷新历史返回、新用户使用路径、Git 缺失降级和 Git for Windows 安装指引。
- `2026-07-15`：完成源码与历史密钥模式检查、本机绝对路径脱敏和 GitHub 仓库公开；`v0.3.9` 成为公开可下载基线。
- `v0.4.0-dev.1`：完成 Windows NSIS 更新状态机、设置页更新卡片、全局提示、Release 元数据、draft 发布 workflow、错误脱敏和 updater 专项校验；未签名构建只检查更新并引导手动安装。
- `v0.4.0`：发布首个 updater-enabled 公开正式版；Release 同时提供安装包、`latest.yml`、blockmap 和 SHA256，作为后续签名版升级验收的安装起点。
- `v0.4.1-dev.2`：开启临时未签名自动升级；固定 stable Release 源，允许自动下载和用户确认后的重启安装，禁用退出时安装并展示 SmartScreen 风险。
- `v0.4.1`：发布临时未签名自动升级引导版；`v0.4.0` 用户需手动安装一次，从本版本开始为后续连续版本自动升级验收提供起点。
- `v0.4.2-dev.1`：参考 LarkSync 改造安装交接；保留 GitHub stable feed、`latest.yml` SHA512 和下载进度，用户确认后由独立 Windows helper 等待旧进程退出，再运行 NSIS 并重启应用。
- `v0.4.2`：发布首个外部安装 helper 正式版；当前用户的临时 Windows 计划任务负责跨主进程退出交接，并在安装结束时自删除。
- `v0.4.3-dev.1`：修复 Windows 系统通知点击路由；主进程在通知关闭或点击前保活通知对象，点击后统一进入独立通知页，关联业务页继续由通知详情按钮打开。
- `v0.4.3`：发布通知跳转修复正式版，并作为首个从 `v0.4.2` 使用外部安装 helper 自动升级的连续稳定版。
- `v0.4.4-dev.1`：升级任务改由无控制台的 `wscript.exe` 启动器托管隐藏 PowerShell worker；通知跳转增加 key 深链接和无重载 `app:navigate`，系统通知与顶部单条详情均能选中对应消息。
- `v0.4.4`：发布升级窗口隐藏和通知详情深链接修复正式版。
- `v0.4.5-dev.1`：确认正式 Windows 安装版的启动与周期检查、发现版本后的自动下载链路；补齐已发现更新时开启自动下载立即开始下载，并精简更新卡片、提示条和详情弹层的解释性小字。
- `v0.4.5`：发布自动下载交互补强与更新界面精简正式版。

## 公开发布后优先级

1. 引导 `v0.4.1` 用户升级到首个外部 helper 正式版；旧版本无法远端获得新 helper，旧链路失败时需手动安装一次。
2. 以 `v0.4.2` 为旧版、后续更高版本为新版，完成检查、下载、用户确认、helper 交接、NSIS 安装、重启和用户数据保留验收。
3. 如后续取得可用代码签名，再接入 Windows 可信 publisher 校验；签名不再作为当前自动升级上线前提。
4. 增加 collector 层测试，优先覆盖 JSONL 解析、`rate_limits` 缺失、仓库归因和计费窗口。

## 下一步

1. 增加脱敏 sample data，方便没有真实 Codex 历史的贡献者复现页面状态。
2. 增加应用内诊断能力，例如打开数据目录、复制脱敏诊断摘要、查看当前 `CODEX_HOME`。
3. 评估 Windows 代码签名和 SmartScreen 说明。
4. 单独排期桌面挂件公开化，验证点击穿透、锁定、隐私模式、多屏和全屏行为。
5. 评估 macOS / Linux 打包支持，但不改变 Windows 优先的公开预览节奏。

## 后续方向

- 周报 / 月报摘要。
- Codex Plugin / MCP 摘要接口。
- 可选账号同步与多设备聚合。
- GitHub 远端元数据补充。
