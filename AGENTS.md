# AGENTS 指南

本文件用于指导后续 AI / 开发者在 `codex-companion` 项目开发中的协作方式。本项目定位为 Codex 专用的非官方开源桌面应用与桌面挂件，开发时必须优先保证数据口径、隐私边界和用户可见体验清晰可靠。

## 1. 基本原则

- 全程使用中文交流、说明和用户可见文案。
- 文档先行，先理解需求、数据来源、指标定义和现有实现，再动手改代码。
- 本项目专注 Codex，不把 Claude Code、Cursor、GitHub Copilot 等其他工具作为首版 provider 规划或实现。
- 以事实来源为准：Codex 本地会话数据、Git 仓库状态、项目文档和代码之间若有冲突，先统一口径，再继续实现。
- 不臆测业务规则、统计口径、字段含义和时间范围；缺字段时先查代码和原始数据，资料没有再补约定或向用户确认。
- 尊重现有技术栈和目录结构，优先沿用项目已有模式，不随意引入新框架或大范围重构。
- 项目对外必须明确为非官方工具，不使用 OpenAI、Codex 官方 logo 或容易造成官方背书误解的视觉资产。

## 2. 工作方式

- 开发前先执行 `git status`，确认当前工作区状态。
- 每个任务开始时先判断：这是规划设计、统计脚本开发、桌面壳实现、挂件实现、数据接线、交互调整、样式优化、文档修订，还是工程清理。
- 根据任务类型阅读相关资料，不把某一份旧说明当成永久唯一入口。
- 改动应保持聚焦，只处理当前任务相关的文件和行为。
- 涉及统计口径、展示字段、数据源路径、页面结构或隐私边界变化时，同步更新对应文档，避免文档和实现分裂。
- 代码完成后至少执行项目当前定义的构建命令；在技术栈尚未建立前，至少执行 `git diff --check` 并说明尚无构建命令。
- 如果改动影响主要交互流程，应启动本地应用或页面做人工检查。
- 每次完成有效修改后都要进行 Git 同步，至少包括：检查变更、更新开发记录、提交 commit；如远端可用，再执行 push。

### 2.1 规划阶段工作流程

- 当用户明确表示“先规划”“先不要写代码”或“后续会有一系列修改”时，先创建或更新正式规划文档。
- 收集阶段每收到一条新需求，只补充计划，不提前改应用实现。
- 规划文档中必须至少记录：创建时间、创建时版本基线、需求主题、影响页面、关键设计约束、相关数据源、实施顺序。
- 对于大型页面、桌面壳或挂件改造，计划不能只写一句待办，必须补足页面规划、数据规划、视觉约束、影响文件和需要联动的前后口径。
- 只有在用户明确发出“开始修改”或同等指令后，才进入正式实施阶段，并在实施过程中持续回写计划状态、开发记录和必要文档。

## 3. 版本号与 Git 同步

### 3.1 版本号规则

- 开发迭代版本：`vX.Y.Z-dev.N`
- 正式发布版本：`vX.Y.Z`
- `X`：重大结构、技术栈或产品方向变化。
- `Y`：新增页面、核心流程、重要能力。
- `Z`：缺陷修复、小范围增强、文档完善。
- `N`：同一个正式版本前的开发迭代序号，从 `1` 递增。
- 不要长期停留在同一个较小基线上机械累加 `dev.N`；当产品能力已跨过一个清晰阶段，例如完成 Codex 数据采集、Codex 账本页、桌面挂件等成体系能力时，应提升 `Y`，必要时提升 `X`。

示例：

- `v0.1.0-dev.1 docs: 初始化规划文档`
- `v0.2.0-dev.1 feat: 接入 Codex token 聚合`
- `v0.2.0 release: 首个可用桌面版本`

### 3.2 Git 同步要求

- 开发前执行 `git status`。
- 如果存在远端且当前任务需要基于最新代码开发，先执行 `git pull --rebase`。
- 提交时只提交本次任务相关文件，不混入无关生成物或历史脏文件。
- commit message 必须带开发版本号，格式：`vX.Y.Z-dev.N type(scope): description`。
- 常用类型：`feat`、`fix`、`docs`、`refactor`、`chore`、`test`。
- 若配置了远端，提交后执行 `git push`。
- 如果本地存在与当前任务无关的改动，保持不动，不要回滚。

## 4. 开发记录

- 每次有效更新都必须更新 `DEVELOPMENT_LOG.md`。
- 开发记录要写清楚版本号、日期、变更类型、开发原因、实现方式、当前结果和验证方式。
- 开发记录必须倒序排列，最新版本放在最上方，方便接手者快速查看当前状态。
- 遗留问题不是必填项；只有确实存在未解决问题、风险或后续动作时才写。
- 记录不能只写“优化”“更新”这类空泛描述，必须能让后续接手者知道实际做了什么。

建议格式：

```md
## [YYYY-MM-DD] vX.Y.Z-dev.N type: 简短说明

- 开发原因：说明本次开发是新增功能、修复 bug、解决 issue、调整规范，还是补齐文档。
- 实现方式：说明具体怎么做，涉及哪些关键文件或模块。
- 当前结果：说明本次完成后的状态和结论。
- 验证方式：说明执行了哪些检查或测试。
- 遗留问题：如有，说明未完成内容、风险或后续建议。
```

## 5. Release 记录

- 每次正式 Release 都必须更新 `RELEASE_NOTES.md`。
- Release 说明必须列出本次 Release 包含哪些开发版本。
- Release 说明必须写清楚主要变更、修复内容、验证结果和升级注意事项。
- 已知问题不是必填项；只有确实存在仍需关注的问题时才写。
- 如果某个开发版本没有进入本次 Release，要明确说明原因。

## 6. 中间资料目录

- 预留 `local_dev_work/` 存放开发过程中的临时分析、草稿、中间结果和排查记录。
- `local_dev_work/` 默认不参与 Git 同步。
- 需要长期保留或交付的内容，应整理后放入正式文档或合适源码目录，再纳入 Git。
- 不要把临时文件散落在仓库根目录。

## 7. 交付标准

- 应用能正常构建，不能留下 TypeScript、lint 或打包错误。
- 用户可见状态和按钮文案应清晰、中文优先。
- 数据展示必须能追溯到 Codex 原始会话数据、Git 原始命令或明确约定。
- 重要流程应具备可验证路径，而不是只做静态摆放。
- 对外文档必须说明本项目是非官方 Codex 桌面伴侣，不隶属于 OpenAI。
- 结果说明要交代改了什么、验证了什么、还有什么风险。

## 8. 工程约束

- 不要把生成产物、临时文件、依赖目录当作源码修改。
- 不要在根目录随意创建散乱文件。
- 不要在缺少上下文时重写架构或替换技术栈。
- 不要为了快速展示而硬编码一套脱离真实 Codex 数据的新统计模型。
- 不要扩展已经明确排除的多 provider 路线，除非当前任务明确要求重新讨论产品方向。

## 9. 修改冲突处理

当发现数据口径、Git 状态、Codex 会话文件或页面展示互相不一致时，按下面方式处理：

1. 先指出冲突点。
2. 判断哪个来源更接近当前任务目标。
3. 必要时补充或修正文档。
4. 再做代码修改。

如果冲突会影响统计定义、业务解释、隐私边界或展示含义，先向用户确认，不要自行决定。
