# Codex Companion 页面设计规格（v0.3.19 草案）

- 创建时间：2026-06-03
- 对应阶段：页面视觉重设前的设计确认
- 当前目标：先确认页面内容、展示方式、尺寸和位置，再进入实际页面开发
- 涉及页面：总览页、Codex 账本页、代码仓库页
- 暂缓范围：桌面挂件细化开发。挂件本轮仅改为默认关闭，后续单独设计。

## 0. 修订记录

- `v0.3`：初步规划总览页、Codex 账本页、代码仓库页，并生成三张设计图初稿。
- `v0.3.1`：先修订全局应用壳层与总览页。重点补齐左侧导航、顶部工具栏、设置入口、总览页时间视角、额度展示边界、数据状态与仓库活跃口径。
- `v0.3.1-image.1`：根据总览页修订稿生成两张总览页状态图，分别用于确认 `自然时间` 和 `计费窗口`。
- `v0.3.2`：根据用户对 `S1-S5` 的反馈修订总览页。重点调整为：设置入口固定到左侧最底部、快照只保留一处、补充图标体系、时间视角改为无边框 `日 / 周 / 月` 文本切换、顶部四卡固定为日周月 Token 与日代码改动、中部卡片改为 `5H 额度窗口` 与 `当前周期用量`、底部表格改为项目周期消耗排行。
- `v0.3.3`：根据新一轮反馈恢复页级 `自然时间 / 计费时间` 视角；顶部四卡避免重复展示数值；中部固定为 `5H` 与 `周` 两张额度卡；底部改为可滚动 `项目概览`，并在不同视角下切换周期口径。
- `v0.3.4`：修正总览页当前生效规则。明确 `自然时间 / 计费时间` 只改变统计口径，不改变顶部四卡字段；中部固定为 `5H / 周额度窗口`；补充“设计图到产品实现”的标准交付流程。
- `v0.3.5`：基于第一页最新实际截图和账本页真实数据边界，重构 `Codex 账本页` 设计。统一应用壳层以已落地总览页为准；保留 `5H / 周 / 可观测月` 三张额度卡；把中部重心改为 `周额度账本` 与真实模型构成；移除旧稿中的伪字段与相对周命名。
- `v0.3.6`：基于第一页最新实际截图和第三页现有采集链路，重构 `代码仓库页` 设计。统一以本地 Git 仓库为唯一主对象；保留顶部筛选与选中仓库联动；移除 `归因 Token`、`文件体量` 与独立远端大卡；左侧从“排行”改为可排序仓库详情表。
- `v0.3.7`：根据第二页新一轮反馈，取消账本页顶部重复的额度三卡，首屏改为 `Token 构成与强度 + 强度摘要`；新增 `日 / 周 / 月`、横轴、纵轴切换规则，并把 `7 日均值 / 峰值时段 / 峰值日 / 单次峰值` 纳入账本页主结构。
- `v0.3.8`：根据第三页新一轮反馈，参考 `dev-ledger` 为代码仓库页补回仓库级 `Token / API 等价成本` 信息，但保持其为下钻信息而非首屏主卡；左侧表新增 `Codex 投入` 列，右侧详情补入累计 Token、累计成本与关联会话摘要。
- `v0.3.9`：根据第三页进一步反馈，取消左表顶部排序按钮，改成点击列表头部排序并支持正序 / 倒序切换；右侧把仓库级投入摘要改成更独立的 `Codex 投入概览`；弱化 `最近提交`，不再占顶部摘要卡，详情内也只保留紧凑的最新少量记录。
- `v0.3.10`：细化第二页 `Token 构成与强度` 与 `强度摘要` 的模块级设计。明确 `日 / 周 / 月` 仅作为局部分析状态；明确 `总 Token / 输入总量 / 缓存输入 / 输出 / 推理 Token` 的包含关系；禁止把 `缓存输入` 和 `推理 Token` 当成与总量并列的独立堆叠段；补齐 tooltip、纵轴和强度摘要公式。
- `v0.3.11`：把第二页强度模块细化单独抽成模块合同，并补齐 `模型构成` 的列表头排序规则；统一约定列表类排序优先采用“点列表头切换降序 / 升序 / 默认”而不是额外一排排序按钮。
- `v0.3.12`：根据第三页图审，收紧代码仓库页壳层一致性要求。左侧边栏与顶部工具栏必须直接复用第一页真实落地壳层；目录视角切换保留在顶栏中部，搜索框与过滤区下移到页面内容首行，不再做成另一套顶栏按钮组。
- `v0.3.13`：根据第二页模块合同重画账本页状态图。把 `日 / 周 / 月` 固定为图表模块级状态，补齐 `窗口累计 / 窗口均值 / 峰值位置 / 峰值用量 / 单次峰值 / 缓存输入占比` 六项摘要，并把 `模型构成` 的排序入口完全收敛到列表头。
- `v0.3.14`：进一步细化第二页模块命名与周期口径。`Token 构成与强度 / 强度摘要 / 模型构成` 分别改为 `Token 走势拆解 / 周期洞察 / 模型贡献`；趋势图保留 `日 / 周 / 月`，右侧两个模块改用 `近7天 / 近30天 / 累计`，明确不使用 `全部`。
- `v0.3.15`：把第二页 `v0.3.14` 的命名与周期规则真正落到状态图。新版状态图补入 `Token 走势拆解 / 周期洞察 / 模型贡献` 的标题替换，以及 `周期洞察 / 模型贡献` 右上角的 `近7天 / 近30天 / 累计` 切换。
- `v0.3.15`：根据第三页继续图审，把代码仓库页统一收回为“定时同步 Git 仓库快照”视角；移除目录视角、扫描目录与重复设置入口；顶栏中部不再强行放无用控件，右侧详情也不再展示本地工作区状态。
- `v0.3.16`：把第二页 `v0.3.15` 状态图落地到项目实现。`DashboardSnapshot.ledger` 新增 `weeklyPeriods / trend / analysis`，渲染层落地 `Token 走势拆解 / 周期洞察 / 周额度账本 / 模型贡献 / 会话归因`，并新增 `npm run verify:ledger` 校验设计图、合同、采集器和页面实现的一致性。
- `v0.3.17`：修正第二页实现密度与程序启动性能。账本页压缩图表空白、提高表格可读行数；启动阶段改为优先显示缓存快照，延迟后台实时采集，自动刷新间隔放宽到 `5` 分钟，并停止当前 UI 不使用的仓库文件体量扫描。
- `v0.3.18`：临时收敛第二页 `Token 走势拆解` 的周期入口。数据层继续保留 `日 / 周 / 月` 趋势桶，但页面当前只开放 `周`；`日 / 月` 入口显示为禁用态，等后续重新确认横轴密度和月视角聚合方式后再恢复；同时给非零 token 分层设置最小可见高度，避免比例差距大时看不出堆叠效果。
- `v0.3.19`：修正全局刷新体验与 Codex 会话采集性能。手动刷新、后台刷新和自动刷新都必须写入 `sourceHealth.refresh`，界面在顶栏下方展示阶段、耗时、新解析文件数和缓存复用数；Codex JSONL 采集改为基于 `size + mtimeMs` 的增量缓存，不再每次重复解析最近 60 天全部会话文件。

## 0.1 当前生效范围

- 当前总览页实现与后续出图，统一以 **第 2 节** 为准。
- 当前账本页设计与后续出图，统一以 **第 3 节** 和 [ui-contract/ledger-v0.1.md](./ui-contract/ledger-v0.1.md) 为准。
- 第二页 `Token 走势拆解 / 周期洞察 / 模型贡献` 的细化交互，以 [ui-contract/ledger-intensity-modules-v0.1.md](./ui-contract/ledger-intensity-modules-v0.1.md) 为准。
- 当前代码仓库页设计与后续出图，统一以 **第 4 节** 和 [ui-contract/repositories-v0.1.md](./ui-contract/repositories-v0.1.md) 为准。
- **第 6 节到第 11 节** 仅保留历史设计图和迭代记录，不再作为当前实现依据。
- 设计图过渡到实际产品的流程，以 [design-to-product-workflow-v0.1.md](./design-to-product-workflow-v0.1.md) 为准。
- 总览页进入实际开发前的最终确认稿，以 [ui-contract/overview-v0.1.md](./ui-contract/overview-v0.1.md) 为准。
- 第二页和第三页的应用壳层基准都不再参考第一页旧设计稿，而是直接参考实际截图：
  - [overview-1360x900-dev61.png](../local_dev_work/overview-1360x900-dev61.png)
  - [overview-billing-1360x900-dev61.png](../local_dev_work/overview-billing-1360x900-dev61.png)

## 1. 总体设计方向

### 1.1 产品气质

Codex Companion 是工作型桌面应用，不做营销式首页。界面应像一个长期打开的工程仪表盘：信息密度高、层级清楚、状态明确、视觉克制。

设计关键词：

- 本机数据仪表盘
- Codex 额度与 Token 账本
- 工程活动追踪
- 非官方、local-first、可验证

### 1.2 视觉原则

- 使用浅色工作台底色，避免整页单一蓝色。
- 保留浅蓝科技感，但加入白、石墨灰、青绿、琥珀色做状态区分。
- 卡片圆角控制在 `8px` 以内。
- 页面区块不堆叠大卡片，核心内容使用表格、条形进度、紧凑摘要和分栏面板。
- 数字使用等宽字体，标签使用清晰中文。
- 所有额度状态必须带来源状态：`已观测`、`未观测`、`待刷新`、`数据过期`。

### 1.3 应用壳层

所有页面必须共用同一套左侧导航与顶部工具栏，避免设计图各自生成出不同的外壳。

建议桌面基准画布：`1440 x 960`

固定结构：

- 左侧导航栏：`248px`，固定全高。
- 顶部工具栏：`72px`，固定在主内容区顶部。
- 主内容区：自适应，左右留白 `28px`，顶部工具栏下方留白 `28px`。
- 页面内容最大宽度：`1180px`。

左侧导航栏分区：

- 顶部品牌区，高度 `92px`：显示 `Codex Companion`、副标题 `非官方 Codex 本机仪表盘`，并以小状态点标注 `本机读取 · 无上传`。
- 主导航区：只展示当前已规划页面，依次为 `总览`、`Codex 账本`、`代码仓库`。
- 系统导航区：底部固定 `设置` 入口，不与主导航混排。
- 底部状态区：显示当前版本、最近快照时间、数据状态。文案必须避免官方背书，例如 `非官方工具`。

设置入口首轮只作为页面入口设计，不展开设置页细节。后续设置页至少覆盖：

- Codex 数据目录。
- 本地仓库根目录。
- 桌面挂件开关与样式。
- 隐私与本地读取说明。
- 价格表、模型映射和数据来源说明。

顶部工具栏分区：

- 左侧页面识别区，宽度约 `360px`：页面标题、页面副标题。
- 中部页面控制区，宽度约 `360px` 到 `420px`：放置当前页面最重要的视角切换或筛选。总览页使用 `时间视角：自然时间 / 计费时间`。
- 右侧数据操作区，宽度约 `360px`：显示数据状态标签、刷新按钮、最近快照时间。
- 顶部工具栏不再重复放置设置入口，设置统一固定在左侧底部。
- 顶栏下方允许一条轻量刷新反馈条，展示 `读取快照 / 增量采集 / 刷新完成 / 刷新失败` 等状态；该反馈条服务所有页面，必须保持紧凑，不抢占页面主叙事。

## 2. 总览页设计

### 2.1 页面目标

回答三个问题：

- 今天 Codex 使用是否正常？
- 当前 5 小时和周额度是否紧张？
- Codex 投入有没有对应到代码产出？

### 2.2 内容结构

总览页当前生效规则如下。

页级时间视角：

- `自然时间`：按自然日、自然周、自然月统计。
- `计费时间`：只改变统计口径，不改变页面结构和顶部四卡字段。
- `自然时间 / 计费时间` 的核心差异是 **周和月的起止定义不同**。
- `今日` 口径当前保持一致；差异主要体现在 `本周` 和 `本月`。

第一行：核心指标带

- 位置：主内容区顶部，占满宽度。
- 高度：`136px`
- 固定展示 4 个等宽指标，不因为时间视角切换而更换字段。

固定四卡：

- `今日 Token`
- `本周 Token`
- `本月 Token`
- `今日代码改动`

四卡展示规则：

- 主数字只显示一次。
- 不再在卡片下方重复展示完整 Token 数字。
- 次级说明使用增减百分比或状态说明：
  - `较昨日 +12.4%`
  - `较上周 +8.7%`
  - `较上月 +15.2%`
  - `较昨日 -3.1%`
- 不在顶部四卡中展示 `会话数`、`事件数`、`等价成本` 等附加字段。
- 不展示趋势曲线。
- Token 数字自动单位：
  - `< 10,000`：整数
  - `>= 10,000 && < 100,000,000`：`万`
  - `>= 100,000,000`：`亿`

四卡口径定义：

- `自然时间`：
  - `今日 Token`：自然日 Token
  - `本周 Token`：自然周 Token
  - `本月 Token`：自然月 Token
  - `今日代码改动`：自然日代码改动
- `计费时间`：
  - `今日 Token`：当前日 Token
  - `本周 Token`：当前周额度窗口 Token
  - `本月 Token`：当前计费月窗口 Token；如果当前数据源尚不可稳定观测，则保持同一卡位，但状态标记为 `未观测` 或 `待补齐`
  - `今日代码改动`：当前日代码改动

第二行：双额度卡

- 左卡：`5H 额度窗口`
- 右卡：`周额度窗口`
- 两张卡在 `自然时间 / 计费时间` 下保持同一结构。

两张额度卡结构：

- 左半区使用圆环。
- 圆环中心显示 **剩余百分比**，不是剩余 Token。
- 圆环下方显示重置时间。
- 右半区显示紧凑数据列表：
  - `Token 用量`
  - `API 等价成本`
  - `代码行数`
  - `会话数`
  - `观测时间`
  - `来源状态`
- 卡片底部显示 Top 3 模型占比条。

额度卡展示边界：

- 当前 Codex 本地快照稳定暴露的是 `used_percent`、`window_minutes`、`resets_at` 和观测时间。
- 当前阶段不展示 `剩余 Token`，因为没有稳定原始字段。
- 当前阶段不展示 `预计可用时长`，因为缺少稳定的连续窗口消耗速度。
- 圆环中心统一显示剩余百分比，并在卡片底部注记：`无剩余 Token 原始字段，圆环中心显示剩余百分比。`

第三行：项目概览

- 底部区域名称固定为 `项目概览`。
- 使用可滚动表格，允许查看全部项目。
- 默认按 Token 降序。
- 保留排序入口：`按 Token`、`按成本`、`按代码行`、`按最近活动`。

表格字段：

- `项目`
- `Token`
- `API 等价成本`
- `代码行数`
- `提交`
- `会话`
- `最近活动`

二级周期切换：

- 当页级视角为 `自然时间` 时，二级切换为 `日 / 周 / 月`。
- 当页级视角为 `计费时间` 时，项目概览暂按当前版本保留 `5H / 周额度` 二级切换。

数据状态展示方式：

- 顶部工具栏右侧显示统一状态标签：`已观测`、`待刷新`、`未观测`、`数据过期`。
- 页面底部使用紧凑说明展示数据来源、session 数、archived 数和仓库数。
- 不再在总览页单独保留 `数据可信度` 大面板。

### 2.3 总览页待确认决策

- 是否接受 `计费时间` 下顶部四卡与 `自然时间` 保持同一字段，只改变周/月统计边界。
- 是否接受 `本月 Token` 在计费时间下先保留字段结构，待月窗口定义补齐后再完全落地。
- 是否接受额度卡圆环中心统一显示剩余百分比，而不是剩余 Token。
- 是否接受项目概览在计费时间下继续保留 `5H / 周额度` 二级切换。

### 2.4 设计图生成提示词

```text
Use case: ui-mockup
Asset type: desktop app page design mockup
Primary request: Create a polished desktop dashboard mockup for the Codex Companion overview page with a consistent app shell and fixed four-card layout.
Scene/backdrop: a light local-first engineering analytics app, not a landing page.
Style/medium: high-fidelity UI mockup, restrained operational dashboard, crisp typography.
Composition/framing: 1440x960 desktop app screenshot, fixed left navigation rail 248px, fixed top toolbar 72px, main dashboard content with four KPI cards, two quota window cards, and a scrollable project overview table.
Color palette: off-white workspace background, graphite text, blue and cyan as accents, green/amber/red for status, avoid one-note blue dominance.
Text (verbatim): "Codex Companion", "非官方 Codex 本机仪表盘", "总览", "Codex 账本", "代码仓库", "设置", "时间视角", "自然时间", "计费时间", "今日 Token", "本周 Token", "本月 Token", "今日代码改动", "5H 额度窗口", "周额度窗口", "项目概览"
Constraints: the top four cards must stay the same under both natural-time and billing-time views; only the statistical boundaries change. No duplicate full token numbers in card helper text. No OpenAI logo, no official branding, no marketing hero, no decorative blobs, no remaining-token value, no estimated available duration, Chinese UI text, 8px card radius.
```

## 3. Codex 账本页设计

### 3.1 页面目标

回答四个问题：

- 最近 Token 走势到底怎么变化？
- 哪些峰值时段、峰值日期和单次会话值得关注？
- 最近几周计费周期的 Codex 投入强度、重置情况和 API 等价价值折算是什么样？
- 哪些真实模型在当前阶段消耗了主要 Token、成本和额度？
- 哪些会话可以被归因到项目或仓库，哪些仍应明确标为 `未归因`？

### 3.2 壳层基准

账本页的左侧导航、顶部工具栏、卡片圆角、留白密度和右上操作区，直接复用第一页最新实际截图，不再以第一页旧设计稿为壳层来源。

统一规则：

- 左侧品牌区、主导航、底部设置入口与第一页一致。
- 顶部工具栏沿用 `左侧页面识别 / 中部页面控制 / 右侧数据操作` 三段式结构。
- 账本页标题、副标题、状态标签、刷新按钮和快照时间的位置与第一页保持一致。
- 顶部工具栏中部不再承载账本页的 `日 / 周 / 月` 视角。
- `日 / 周 / 月` 原设计下沉到 `Token 走势拆解` 模块头部，只作为该分析模块的局部状态；当前实现阶段先只开放 `周`，`日 / 月` 入口保留禁用态。
- `周期洞察` 与 `模型贡献` 不再硬跟 `日 / 周 / 月` 共用同一套周期，而是各自在模块右上角提供 `近7天 / 近30天 / 累计`。

### 3.3 数据展示边界

先看真实数据，再定页面字段。

现有底层已可推导：

- `CodexTokenEvent.timestamp` 驱动的 `按小时 / 按日期 / 按周段` 聚合桶。
- `CodexTokenEvent.tokens` 驱动的：
  - `总 Token`
  - `输入总量`
  - `缓存输入`
  - `缓存输入占比`
  - `输出`
  - `推理 Token`
- 当前必须明确：
  - `总 Token = 输入总量 + 输出`
  - `输入总量 = 原始输入 + 缓存输入`
  - `推理 Token` 是 `输出` 的子集，不是独立第四段
- 基于多日聚合的：
  - `7 日均值`
  - `峰值时段`
  - `峰值用量`
  - `峰值日`
  - `单次峰值`
- 真实模型聚合：
  - 模型名
  - Token
  - API 等价成本
  - 事件数
  - 占比
- 会话归因：
  - 最近时间
  - 会话 ID
  - 项目 / 仓库 / 工作目录
  - 主模型
  - Token
  - API 等价成本
- `v0.2.2-dev.74` 已接入的多周计费周期账本字段：
  - 日期区间：`snapshot.ledger.weeklyPeriods[*].startAt / endAt`
  - 累计已用百分比：`period.quotaEvidence.usedPercent`
  - 周期 Token：`period.tokens.total`
  - API 等价成本：`period.apiCostUsd`
  - 满额周 API 等价折算价值：由 `apiCostUsd / (usedPercent / 100)` 推导，只作为估计值展示
  - 会话数：`period.sessions`
  - 重置次数：`period.quotaEvidence.resetCount`
  - 观测次数：`period.quotaEvidence.observations`
  - 趋势桶：`snapshot.ledger.trend.day / week / monthByDate / monthByWeek`
  - 独立分析周期：`snapshot.ledger.analysis.sevenDays / thirtyDays / cumulative`

当前不应展示：

- 再做一排 `5H / 周 / 可观测月` 大卡，因为它和第一页主叙事重复。
- `剩余 Token`，因为当前没有稳定原始字段。
- 固定月额度，因为当前没有可验证的固定月额度来源。
- 套餐余额、积分余额、官方套餐价格或任何看起来像官方账单的字段。
- 用自然月伪装的计费月窗口。
- 旧稿中的伪数据：`user_01`、`平均响应(s)`、`已完成` 等非真实来源字段。
- `上上周 / 上上上周` 这类相对命名。

仅适合次级展示或悬停展示：

- 周期开始与结束的 `小时:分钟`。
- 额度重置分段详情。
- 模型贡献的长尾尾项明细。
- 工作目录全路径。

### 3.4 内容结构

第一行：Token 走势拆解 + 周期洞察

- 左侧主卡：`Token 走势拆解`
  - 占 `68%` 宽度，高度 `356px`
  - `日 / 周 / 月` 视角切换放在模块头部，而不是页面顶部工具栏；当前只允许 `周`，`日 / 月` 暂不可用
  - 卡片头部保留两个次级切换：
    - `横轴`
    - `纵轴`
  - `横轴` 只显示当前视角可用选项；当前实现因只开放 `周`，实际固定为 `日期`
    - `日`：`小时`
    - `周`：`日期`
    - `月`：`日期 / 周段`
  - `纵轴` 固定支持：
    - `总 Token`
    - `输入总量`
    - `缓存输入`
    - `缓存输入占比`
    - `输出`
    - `推理 Token`
  - 图形规则：
    - `总 Token` 使用 `原始输入 + 缓存输入 + 输出` 三段堆叠，`推理 Token` 仅作为 `输出` 段内部子集标记
    - `输入总量` 使用 `原始输入 + 缓存输入` 两段堆叠
    - 单一 Token 指标切换后退化为单序列柱图
    - `缓存输入占比` 切换后改为百分比柱图，纵轴固定 `0% - 100%`
    - 非零分层允许设置最小可见高度，避免比例过小导致视觉上像未分层；真实值仍以 tooltip 为准
- 右侧摘要卡：`周期洞察`
  - 占 `32%` 宽度，高度 `356px`
  - 右上角提供 `近7天 / 近30天 / 累计`
  - 不使用 `全部`
  - 只跟随自己的周期范围，不跟随左侧图表 `日 / 周 / 月`
  - 不跟随纵轴漂移
  - 固定显示 6 个高价值摘要位：
    - `窗口累计`
    - `窗口均值`
    - `峰值位置`
    - `峰值用量`
    - `单次峰值`
    - `缓存输入占比`
  - `窗口均值` 在不同视角下分别解释为：
    - `近7天`：7 日均值
    - `近30天`：30 日均值
    - `累计`：观测期日均
  - `单次峰值` 按单个会话总量得出，不展开原始对话内容

第二行：周额度账本与模型贡献

- 左侧：周额度账本表，占 `64%` 宽度，高度 `372px`
- 右侧：模型贡献面板，占 `36%` 宽度，高度 `372px`

左侧周额度账本：

- 表头不写 `上周 / 上上周 / 上上上周`，直接按日期区间展示。
- 默认字段：
  - `起止日期`
  - `累计已用`
  - `Token`
  - `API 等价成本`
  - `满额周折算`
  - `会话`
  - `观测 / 重置`
- 行级规则：
  - `起止日期` 首屏只显示 `MM/DD - MM/DD`。
  - 鼠标悬停时再显示完整 `YYYY-MM-DD HH:mm - YYYY-MM-DD HH:mm`。
  - 如果某周期存在重置，`观测 / 重置` 列显示 `重置 1 次` 等短 badge。
  - 如果 `累计已用` 超过 `100%`，允许显示 `117%` 这类累计结果，不强行截断。
  - `满额周折算` 文案必须明确是 `API 等价折算`，不是官方套餐定价。

右侧模型贡献：

- 右上角提供 `近7天 / 近30天 / 累计`
- 不使用 `全部`
- 默认按 `Token` 倒序
- 点击列头切换排序：
  - 第一次点击某列：按该列降序
  - 第二次点击同列：按该列升序
  - 第三次点击同列：回到默认排序
- 不再额外放 `Token / 成本 / 事件数` 按钮组
- 使用水平堆叠条，不使用大面积饼图
- 模型名使用真实可用标签：
  - 优先使用 `GPT-5.5（超高）`、`GPT-5.4（低）` 这类带推理强度的真实标签
  - 如果当前实现阶段只能稳定得到原始模型 ID，则退化为 `gpt-5.5`、`gpt-5.4`、`gpt-5.3-codex-spark`
- 每行显示：
  - 模型名
  - 占比条
  - 主指标值
  - 次级值
- 不使用 `codex-pro`、`codex-mini` 这类旧稿假名。

第三行：会话归因表

- 位置：底部，占满宽度。
- 高度：`320px`
- 字段：
  - 最近时间
  - 会话 ID 短码
  - 项目 / 仓库
  - 主模型
  - Token
  - API 等价成本
- 展示规则：
  - 项目已归因时优先显示项目 / 仓库名。
  - 没有仓库归因时显示 `未归因`，工作目录只作为次级信息或悬停信息。
  - 不展示用户、会话状态、平均响应时长、调用次数等当前没有稳定来源的字段。
  - 明确不展开原始对话内容，只展示聚合字段。

### 3.5 设计图生成提示词

```text
Use case: ui-mockup
Asset type: desktop app page design mockup
Primary request: Create a refined desktop ledger page mockup for Codex Companion that reuses the exact shell language of the latest shipped overview screenshot rather than an older concept mockup.
Scene/backdrop: a local-first Codex quota and token ledger with production-like density, not a marketing dashboard.
Style/medium: high-fidelity desktop app mockup, restrained operational software, crisp typography, same visual family as the current overview implementation.
Composition/framing: 1440x960 desktop app screenshot, same left navigation rail and top toolbar as the shipped overview page, top toolbar center stays minimal without day/week/month tabs, first row with a large token composition and intensity chart on the left plus a compact signal summary card on the right, second row with a weekly billing ledger table on the left and a real-model composition panel on the right, third row with a session attribution table.
Color palette: off-white workspace, graphite text, blue/cyan/green accents, subtle borders, same tone and spacing as the actual overview screenshot.
Text (verbatim): "Codex 账本", "构成强度、周期细账与会话归因", "Token 走势拆解", "周期洞察", "周额度账本", "模型贡献", "会话归因", "窗口累计", "窗口均值", "峰值位置", "峰值用量", "单次峰值", "缓存输入占比", "累计已用", "API 等价成本", "满额周折算", "事件数", "横轴", "纵轴", "近7天", "近30天", "累计"
Constraints: reuse the overview shell, do not include repeated top quota cards, remaining token count, fixed monthly quota, user names, average response time, or completion status; place day/week/month only inside the trend chart header together with selectable x-axis and y-axis; the token chart uses only three stack segments for 原始输入 / 缓存输入 / 输出, and treats 推理 Token as a marker inside 输出 rather than a fourth stacked series; the summary card uses the six fixed metrics 窗口累计 / 窗口均值 / 峰值位置 / 峰值用量 / 单次峰值 / 缓存输入占比 and adds its own 近7天 / 近30天 / 累计 switch; the model contribution card also uses 近7天 / 近30天 / 累计 and clickable column headers for sorting instead of a separate button row; do not use the label 全部; display date ranges instead of labels like 上上周; use real model names such as GPT-5.5 and GPT-5.4 or gpt-5.5 / gpt-5.4 / gpt-5.3-codex-spark; no OpenAI logo, no official branding, no decorative hero, Chinese UI text, 8px card radius.
```

## 4. 代码仓库页设计

### 4.1 页面目标

回答三个问题：

- 当前定时同步的 Git 仓库里，哪些仓库近期最活跃？
- 选中的 Git 仓库最近做了什么，最近一次提交和同步上下文是什么？
- Codex 本地会话最近主要落在哪些仓库，并对应了多少累计 Token 与 API 等价成本，但不把它们抬成首屏主数字？

### 4.2 内容结构

第三页不再沿用旧稿“本地仓库 + 排行 + 远端大卡”的表达，而是改成下面这套更贴近当前方向的数据结构：

- 页面主对象统一为 **定时同步的 Git 仓库快照**，不是本地文件夹视图，也不是 GitHub 云端仓库看板。
- 顶部不再保留 `目录视角`、`扫描目录` 这类本地目录语义；当前页只保留仓库搜索、列表排序和右侧联动详情。
- 顶部主叙事仍不使用 `归因 Token` 和 `API 成本` 做 KPI 主卡，但允许在左表和右侧详情中展示仓库级 `累计 Token / 累计 API 成本`。
- 页面内不再单独放一张 `GitHub 远程仓库信息` 大卡；远端仓库标识只作为选中仓库的一条辅助信息出现。
- 左侧中部卡从“仓库排行”改为 **可排序的仓库详情表**；右侧继续保留单仓库详情，与左侧选中态联动。

壳层约束：

- 左侧边栏必须与第一页真实截图逐项一致：
  - 同一 `BrandMark`
  - 同一标题、副标题和 `本机读取 · 无上传`
  - 同一主导航图标与激活态
  - 同一底部 `设置` 卡与版本 / `非官方工具` 文案
- 左侧主导航只能保留 `总览 / Codex 账本 / 代码仓库`，不得再额外出现一条 `设置` 导航项。
- 顶部工具栏必须回到第一页的壳层语言：
  - 左：`代码仓库` + 副标题 inline 信息组
  - 中：保留和第一页一致的留白节奏，但此页不强行放目录切换或无意义 tabs
  - 右：只保留第一页同风格的 `状态 / 刷新 / 快照 HH:mm`
- 顶栏右侧不得额外出现独立的筛选按钮、搜索按钮或“生成快照”按钮。
- 顶栏中部不再额外放 `目录视角`、`扫描目录`、`设置` 或其他补丁式控件。
- 搜索框必须下移到内容区首行，不再伪装成顶栏操作。

第一行：内容区过滤

- 左侧：搜索框。
- 右侧：轻量排序提示，例如 `默认按最近提交排序 · 点击表头切换`。
- 当前页不再出现 `扫描目录` chips、`目录视角` tabs 或本地路径分组控件。

第二行：顶部四个摘要卡

- `今日改动`
- `近 7 日改动`
- `本月改动`
- `同步仓库`

约束：

- 四卡直接复用第一页当前已落地的卡片比例、圆角、阴影、状态徽标和信息密度。
- 第四卡改为 `同步仓库` 或同义表达，主值显示当前同步仓库总数；辅助文案可补充其中有 Codex 归因记录的仓库数。
- 顶部不再出现 `归因 Token`、`API 等价成本`、`文件体量` 这类字段；这些信息只出现在中部工作台。

第三行：仓库详情表 + 单仓库详情

- 左侧：`Git 仓库详情`，占 `58%` 到 `60%` 宽度。
- 右侧：`已选仓库`，占 `40%` 到 `42%` 宽度。
- 高度：两栏统一控制在约 `520px` 到 `560px`，只允许内部容器滚动，不允许整页纵向滚动。

左侧仓库详情表字段：

- 仓库
- 今日改动
- 近 7 日改动
- 本月改动
- Codex 投入
- 最近提交

左侧表约束：

- 去掉旧稿中的 `#` 排名列，不再做固定“排行”语义。
- 不再使用一排排序 chips / 按钮。
- 默认排序为 `最近提交` 倒序，也就是最新提交的仓库默认排在最前。
- 用户点击表头字段即可切换排序：第一次切换到该字段的默认顺序，再次点击同一字段时切换正序 / 倒序。
- 允许点击排序的字段为：`今日改动 / 近 7 日改动 / 本月改动 / Codex 投入 / 最近提交`。
- 默认分支折叠进仓库名下方的辅助信息，并与远端仓库标识一起显示，不再单独占一列。
- `Codex 投入` 列主值显示累计 Token，辅值显示 API 等价成本；呈现方式参考 `dev-ledger` 的紧凑双层信息，而不是拆成两个宽列。
- 仍不展示 `风险等级`、`文件体量`。

右侧单仓库详情：

- 头部信息：仓库名、远端仓库标识、默认分支、最近提交时间。
- 指标块：`今日改动 / 近 7 日改动 / 本月改动`。
- `Codex 投入概览`：`累计 Token / 累计 API 等价成本 / 关联会话 / 最近 Codex 活动`。
- 最近提交：只保留紧凑的最新 `1` 到 `2` 条提交，或者仅保留“最近一次提交 + 查看全部”入口，不再展示长列表。
- 仓库状态：`远端地址（如存在）` 与最近同步时间。

右侧详情约束：

- 不再放语言分布环图或文件体量图。
- 不把远端地址升级成独立功能区；它只是辅助识别信息。
- 如果当前仓库没有远端，只显示 `未配置远端`，不出现空的大卡占位。
- 不再展示 `工作区改动`、`未提交文件` 这类本地工作树状态。
- 当前数据边界下，`Token / 成本` 统一使用累计口径；不伪造 `30 天成本`、`30 天 Token` 等当前合同里还没有的仓库级周期字段。
- `Codex 投入概览` 不应挤成一整条生硬的横向统计行；更适合做成右侧详情中的独立浅色内嵌卡或 `2 x 2` 紧凑指标矩阵，和上方活动指标区形成两段式层级。
- `最近提交` 的价值主要是验证仓库新鲜度和补充当前工作上下文，不是页面主叙事；因此只保留最小必要信息，不再让它占据过大视觉面积。

第四行：紧凑数据来源说明

- 只保留一条紧凑页脚说明，明确 `数据来源：Codex 本地 sessions、archived_sessions、定时同步 Git 快照`。
- 仓库总数和最近同步时间可以作为页脚紧凑说明的一部分出现，不再单独做大块“数据源根目录”展示。
- 必须明确：当前页面不读取 GitHub PR、Issue、Actions、Stars 等云端元数据。

### 4.3 设计图生成提示词

```text
Use case: ui-mockup
Asset type: desktop app page design mockup
Primary request: Create a polished desktop Git repository activity page mockup for Codex Companion, reusing the exact same left sidebar and top toolbar shell as the latest shipped overview page.
Scene/backdrop: periodically synced Git repository activity and Codex local session linkage, focused on engineering work rather than billing or cloud metadata.
Style/medium: high-fidelity desktop app mockup, practical engineering operations dashboard, same blue-white shell and card rhythm as the latest overview screenshot.
Composition/framing: 1440x960 desktop app screenshot, exact same left navigation rail and top toolbar language as the shipped overview page. The left rail keeps only overview, ledger, and repositories in the main navigation, with a single settings card fixed at the bottom. Top toolbar keeps the same shell rhythm as the overview page: title/subtitle on the left, clean reserved whitespace in the center with no tabs, and status / refresh / snapshot time on the right. A search row below the toolbar shows a repository search box on the left and a subtle sorting hint on the right. Then four compact summary cards, a sortable repository detail table on the left, a selected repository detail panel on the right, and a compact footer note.
Color palette: soft white and pale blue base, graphite text, restrained blue/cyan accents, green only for positive status, avoid dark mode, avoid purple bias.
Text (verbatim): "代码仓库", "同步 Git 仓库活动与 Codex 投入", "搜索仓库", "默认按最近提交排序", "今日改动", "近 7 日改动", "本月改动", "同步仓库", "Git 仓库详情", "Codex 投入", "已选仓库", "Codex 投入概览", "累计 Token", "累计 API 等价成本", "关联会话", "最近 Codex 活动", "远端仓库"
Constraints: no OpenAI logo, no official branding, no marketing page, no oversized hero, no token-focused top KPI cards, no file-size chart, no separate GitHub remote card, no fixed ranking number column, no row of sort buttons above the left table, no custom alternate sidebar, no duplicate settings item inside the main navigation, no directory-view tabs, no scan-directory chips, Chinese UI text, compact readable table, exact same shell language as the latest overview page.
```

## 5. 本轮确认标准

设计图确认时重点看：

- 左侧导航、顶部工具栏和设置入口是否在所有页面中保持一致。
- 页面信息是否完整。
- 各区块主次是否符合实际使用优先级。
- 表格、指标、状态标签是否足够清晰。
- 页面是否符合工作型桌面应用，而不是营销页。
- 是否需要调整整体密度、色彩、导航位置或单页内容范围。

确认后再进入实现阶段，并按本文档逐项改造实际页面。

后续确认方式调整为“先看图、再用少量编号反馈”：

- `S1`：左侧导航、顶部工具栏、设置入口是否确认。
- `S2`：页面主次结构和信息密度是否确认。
- `S3`：关键指标卡的字段、口径和切换状态是否确认。
- `S4`：中部分析面板是否确认。
- `S5`：底部表格或列表是否确认。

用户反馈时可直接写类似：`S1 OK，S3 重出，S5 表格太挤`。

## 6. 设计图初稿

本轮生成三张页面设计图，先用于确认页面信息结构与视觉方向：

- 总览页设计图：[overview-page.png](./assets/design/v0.3/overview-page.png)
- Codex 账本页设计图：[ledger-page.png](./assets/design/v0.3/ledger-page.png)
- 代码仓库页设计图：[repositories-page.png](./assets/design/v0.3/repositories-page.png)

初步观察：

- 总览页的整体层级清晰，但左侧导航出现了部分未规划页面项，且顶部工具栏与其他页面不一致；该图应按 `v0.3.1` 总览页规格重出。
- Codex 账本页的信息结构较贴近本文档，可作为后续实现参考。
- 代码仓库页的表格与详情分栏清晰，可作为后续实现参考，但 GitHub 远端区域需在实际开发时继续保持“后续可选能力”口径。

## 7. 总览页 v0.3.1 状态图

根据 `v0.3.1` 总览页规格，生成两张总览页状态图：

- 自然时间状态图：[overview-natural-time.png](./assets/design/v0.3.1/overview-natural-time.png)
- 计费窗口状态图：[overview-billing-window.png](./assets/design/v0.3.1/overview-billing-window.png)

当前观察：

- 自然时间图的应用壳层、设置入口、时间视角切换、模型用量分析和投入产出对照整体可作为确认基础。
- 自然时间图中额度健康表格的已用 / 剩余百分比位置略容易误读，后续实现或重出图时需要把已用、剩余和进度条关系做得更明确。
- 计费窗口图能表达四个指标卡的计费窗口口径，也明确避免了剩余 Token 和预计可用时长。
- 计费窗口图中投入产出对照表的 Git 产出字段出现了“提交 / 文件”混合写法，后续应统一为改动行数、提交数或拆成两个字段。

## 8. 总览页 v0.3.2 修订规格

本节覆盖 `2.2` 中的总览页结构，用于下一轮出图确认。

### 8.1 S1：应用壳层与图标

左侧边栏：

- 顶部品牌区显示应用图标、`Codex Companion`、`非官方 Codex 本机仪表盘` 和 `本机读取 · 无上传`。
- 主导航只保留 `总览`、`Codex 账本`、`代码仓库`。
- `设置` 固定在左侧边栏最底部，作为最后一个可点击项。
- 左侧不再显示 `最近快照`，避免与顶部工具栏重复。
- 左侧底部只保留版本号和非官方状态，例如 `v0.2.2-dev.3`、`非官方工具`。

顶部工具栏：

- 右侧保留唯一快照信息：`快照 14:32`。
- 右侧同时显示数据状态标签和刷新按钮。
- 时间视角不使用外框容器，改为无边框文本切换：`日`、`周`、`月`。
- 当前选中的时间视角使用蓝色文字、下划线或浅色底，不使用大号分段控件边框。

图标体系：

- 应用图标：抽象 `C` 形本地节点 / 六边形仪表盘组合，不使用 OpenAI 标识，不使用官方 Codex 资产。
- 主导航图标：
  - `总览`：四象限仪表盘图标。
  - `Codex 账本`：账本 / 列表图标。
  - `代码仓库`：文件夹 + Git 分支图标。
  - `设置`：齿轮图标。
- 页面区块图标：
  - 5H 额度窗口：计时器 / 环形额度图标。
  - 当前周期用量：日历周期图标。
  - 项目消耗排行：项目文件夹 / 排行图标。
- 图标尺寸：应用图标 `32px`，导航图标 `20px`，区块图标 `18px`。
- 图标风格：统一 2px 线性圆角描边，活动项使用蓝色填充或浅蓝底；非活动项使用石墨灰描边。

### 8.2 S2：整体布局

当前 `v0.3.1` 的整体密度和视觉方向保留。

### 8.3 S3：顶部指标卡

顶部四张指标卡在 `日 / 周 / 月` 切换时保持结构一致，不再像 `v0.3.1` 那样切换成完全不同的卡片组。

固定四卡：

- `今日 Token`
- `本周 Token`
- `本月 Token`
- `今日代码改动`

展示规则：

- 不再展示趋势曲线；趋势或细分图放到后续详细页面。
- 每张卡片展示主数字、自动单位、辅助信息和来源状态。
- Token 自动单位：
  - 小于 `10,000`：直接显示整数，例如 `8,420`
  - 大于等于 `10,000` 且小于 `100,000,000`：显示 `万`，例如 `12.8 万`
  - 大于等于 `100,000,000`：显示 `亿`，例如 `1.24 亿`
  - 鼠标悬停或二级说明中保留完整数值。
- 辅助信息建议：
  - Token 卡：会话数、API 等价成本、事件数。
  - 今日代码改动卡：改动行数、提交数、未提交改动。
- 当前选中的 `日 / 周 / 月` 只影响高亮态和下方模块口径，不改变四卡字段。

### 8.4 S4：中部指标卡

中部不再拆成 `额度健康` 与 `模型用量分析` 两类模块，改为两个大卡：

- 左卡：`5H 额度窗口`
- 右卡：`当前周期用量`

左卡 `5H 额度窗口` 展示：

- 已用百分比、剩余百分比。
- 当前 5H 窗口 Token。
- 恢复时间、观测时间、来源状态。
- 当前窗口 Top 3 模型及 Token 占比。
- API 等价成本。
- 明确提示：`无剩余 Token 原始字段，仅展示百分比`。

右卡 `当前周期用量` 展示：

- 标题随时间视角变化：
  - 日视角：`今日用量`
  - 周视角：`本周用量`
  - 月视角：`本月用量`
- 周期 Token、API 等价成本、会话数、事件数。
- Top 3 模型及 Token 占比。
- 周期内代码改动行数和提交数。
- 如未来观测到月额度字段，再考虑把月视角右卡升级为 `月额度窗口`；当前版本不这样命名。

### 8.5 S5：项目周期消耗排行

底部不再使用 `投入与产出对照`，改为 `项目消耗排行`。

表格跟随 `日 / 周 / 月` 时间视角变化：

- 日视角：展示今日项目消耗。
- 周视角：展示本周项目消耗。
- 月视角：展示本月项目消耗。

字段建议：

- `项目`
- `Token`
- `API 等价成本`
- `会话`
- `主模型`
- `代码行数`
- `提交`
- `最近活动`

排序：

- 默认按 Token 降序。
- 可切换为按成本、代码行数、最近活动排序。

### 8.6 v0.3.2 出图策略

本轮先生成两类图：

- 图标体系稿：确认应用图标、导航图标、区块图标风格。
- 总览页月视角稿：确认应用壳层、无边框时间切换、固定四卡、中部双卡和项目周期消耗排行。

月视角稿确认后，如整体方向成立，再生成日视角和周视角补充图。

## 9. 总览页 v0.3.2 状态图

根据用户对 `S1-S5` 的反馈，生成以下设计图：

- 图标体系稿：[icon-system.png](./assets/design/v0.3.2/icon-system.png)
- 总览页月视角稿：[overview-month.png](./assets/design/v0.3.2/overview-month.png)

当前观察：

- 图标体系稿整体符合 `2px` 线性圆角描边风格，可作为后续 SVG 重建的方向稿。
- 图标体系稿中的应用图标采用本地节点环绕六边形仪表盘，未使用 OpenAI 或官方 Codex 资产，符合非官方边界。
- 总览页月视角稿已将设置放到左侧最底部，并移除了左侧快照时间，快照只保留在顶部右侧。
- 总览页月视角稿已将时间视角改为无边框 `日 / 周 / 月` 文本切换。
- 总览页月视角稿顶部四卡保持固定字段，且去掉趋势曲线，Token 数字使用 `万` 单位与完整数值辅助说明。
- 中部已改为 `5H 额度窗口` 和 `本月用量` 两张大卡，避免按“额度 / 模型”这种抽象分类拆分。
- 底部已改为 `项目消耗排行`，表格随月视角展示项目 Token、成本、会话、主模型、代码行数、提交和最近活动。
- 当前风险：图标体系稿与总览页稿里的应用图标细节并非完全同一枚图标；确认方向后，实际实现时应锁定一枚 SVG 源图标并在所有位置复用。

## 10. 总览页 v0.3.3 覆盖规格

本节覆盖 `8` 中的总览页 `v0.3.2` 规则，用于下一轮重出 `自然时间` 与 `计费时间` 两张图。

### 10.1 页级时间视角

- 页级视角恢复为 `自然时间` 和 `计费时间`。
- 顶部工具栏中部使用无边框文本切换，不使用分段控件外框。
- `自然时间` 视角主要回答今天 / 本周 / 本月的 Token 与代码活动。
- `计费时间` 视角主要回答当前 `5H` 和 `周额度窗口` 的使用情况。

### 10.2 顶部四个指标卡

四个指标卡继续保留统一布局和统一视觉结构，但不再在卡片上下重复展示同一个 Token 数值。

`自然时间` 视角：

- `今日 Token`
- `本周 Token`
- `本月 Token`
- `今日代码改动`

`计费时间` 视角：

- `当前 5H Token`
- `5H 剩余`
- `当前周额度 Token`
- `周额度剩余`

卡片展示规则：

- 主数字只出现一次。
- 次级说明回到增减百分比或状态说明，不再重复完整数值。
- Token 卡次级说明优先用对比变化：
  - `较昨日 +12.4%`
  - `较上周 +8.7%`
  - `较上月 +15.2%`
- 代码改动卡次级说明使用改动趋势：
  - `较昨日 -3.1%`
- 剩余卡如果没有合理同比对象，则使用恢复或观测说明：
  - `恢复 今天 19:32`
  - `观测 14:32`
- Token 数字自动单位：
  - `< 10,000`：整数
  - `>= 10,000 && < 100,000,000`：`万`
  - `>= 100,000,000`：`亿`

### 10.3 中部两张额度卡

中部固定展示两张卡，不随页级视角切换而更换模块类型：

- 左卡：`5H 额度窗口`
- 右卡：`周额度窗口`

两张卡采用相似结构：

- 左侧使用圆环或环形进度。
- 圆环中心显示 `剩余百分比`，例如 `32%`、`58%`。
- 圆环下方显示重置时间，例如 `今天 19:32 重置`、`周日 24:00 重置`。
- 右侧展示紧凑数据列表：
  - `Token 用量`
  - `API 等价成本`
  - `代码行数`
  - `会话数`
  - `观测时间`
  - `来源状态`
- 卡片底部显示 Top 3 模型占比条。
- 明确注记：`当前无剩余 Token 原始字段，圆环中心显示剩余百分比。`

### 10.4 底部项目概览

底部区域改名为 `项目概览`，不再使用 `项目消耗排行`。

展示方式：

- 使用可滚动表格，允许查看所有项目。
- 默认按 Token 降序。
- 保留排序入口：`按 Token`、`按成本`、`按代码行`、`按最近活动`。

页级视角与表格口径关系：

- 当页级视角为 `自然时间` 时，表格顶部提供二级切换：`日 / 周 / 月`。
- 当页级视角为 `计费时间` 时，表格顶部提供二级切换：`5H / 周额度`。

表格字段：

- `项目`
- `Token`
- `API 等价成本`
- `代码行数`
- `提交`
- `会话`
- `最近活动`

说明：

- `主模型` 暂时从表格中移除，避免信息过密。
- `事件数` 暂时不进入顶部四卡和项目表，保持总览页简洁。

### 10.5 v0.3.3 出图策略

本轮重出两张总览页图：

- `自然时间` 视角图：确认顶部四卡的自然时间口径、中部 5H/周额度卡和底部 `日/周/月` 项目概览。
- `计费时间` 视角图：确认顶部四卡的计费窗口口径、中部同结构 5H/周额度卡和底部 `5H/周额度` 项目概览。

## 11. 总览页 v0.3.3 状态图

根据最新反馈，生成以下两张总览页图：

- 自然时间状态图：[overview-natural-time.png](./assets/design/v0.3.3/overview-natural-time.png)
- 计费时间状态图：[overview-billing-time.png](./assets/design/v0.3.3/overview-billing-time.png)

当前观察：

- 两张图共用了同一套应用壳层，左侧设置都固定在最底部，快照时间只在顶部右侧出现一次。
- 顶部 `自然时间 / 计费时间` 已恢复为页级视角，且使用无边框文本切换。
- 顶部四卡已去掉重复的完整数值，改为主数字 + 增减百分比或恢复说明。
- 自然时间图中，顶部四卡分别是 `今日 Token`、`本周 Token`、`本月 Token`、`今日代码改动`，符合自然时间口径。
- 计费时间图中，顶部四卡分别是 `当前 5H Token`、`5H 剩余`、`当前周额度 Token`、`周额度剩余`，且结构与自然时间图保持一致。
- 中部固定为 `5H 额度窗口` 和 `周额度窗口` 两张卡，圆环中心显示剩余百分比，圆环下方显示重置时间，右侧显示 Token、成本、代码行数、会话数等紧凑数据。
- 底部已从 `项目消耗排行` 调整为可滚动 `项目概览`，并根据页级视角切换二级周期：
  - 自然时间图：`日 / 周 / 月`
  - 计费时间图：`5H / 周额度`

## 12. Codex 账本页 v0.3.15 状态图

根据第一页最新实际截图壳层、第二页模块合同和当前真实数据边界，生成以下账本页状态图：

- 账本页状态图：[ledger-page.png](./assets/design/v0.3.15/ledger-page.png)

当前观察：

- 左侧导航、顶部工具栏、右上状态/刷新/快照与第一页实际截图统一，不再沿用旧稿中不一致的壳层。
- 顶部不再重复 `5H / 周 / 月额度` 三张主卡，首屏主叙事改为 `Token 走势拆解 + 周期洞察`。
- 顶部工具栏中部不再承载 `日 / 周 / 月`；时间范围被收回到 `Token 走势拆解` 模块头部，只影响该图。
- `Token 走势拆解` 主卡只允许三段堆叠：`原始输入 / 缓存输入 / 输出`；`推理 Token` 只作为 `输出` 内的子集标记出现，不再误画成第四段。
- `周期洞察` 固定为 `窗口累计 / 窗口均值 / 峰值位置 / 峰值用量 / 单次峰值 / 缓存输入占比` 六项，不再混入会随纵轴漂移的临时指标名。
- `周期洞察` 与 `模型贡献` 都已经在右上角补入 `近7天 / 近30天 / 累计`，且不使用 `全部`。
- 中部左侧改为 `周额度账本`，按日期区间展示最近几周计费周期，不再出现 `上上上周` 这类相对命名。
- `累计已用` 与 `满额周折算` 被提升为账本页核心字段，用来体现额度强度和 API 等价价值；但文案必须清楚说明其为折算值，不是官方套餐定价。
- 模型贡献使用真实模型名称，且排序入口只放在表头本身，默认 `Token` 倒序，不再额外放 `Token / 成本 / 事件数` 切换按钮。
- 新图不再只是布局骨架，而是当前第二页命名、周期和排序语义的正式对照图。
- 会话归因只保留当前真实可展示的聚合字段，不再出现用户、平均响应时长、完成状态等无来源字段。

### 12.1 v0.3.16 实现落地说明

- 实现版本：`v0.2.2-dev.75`
- 数据合同：`src/shared/contracts.ts` 已扩展 `LedgerTimeBucket`、`LedgerAnalysisPeriod` 和 `DashboardSnapshot.ledger.weeklyPeriods / trend / analysis`。
- 数据生成：`src/main/collectors/dashboardCollector.ts` 已按真实 `CodexTokenEvent` 生成小时、日期、周段桶，并生成 `近7天 / 近30天 / 累计` 的洞察与模型贡献。
- 页面实现：`src/renderer/App.tsx` 已移除第二页旧的重复额度三卡，改为 `Token 走势拆解 / 周期洞察 / 周额度账本 / 模型贡献 / 会话归因`。
- 视觉实现：`src/renderer/styles.css` 已补齐账本页专属网格、柱形图、洞察指标和表格样式，壳层继续复用第一页实际实现。
- 一致性校验：`npm run verify:ledger` 检查设计图、合同、采集器、渲染层和关键禁用项。

### 12.2 v0.3.18 当前修正说明

- `Token 走势拆解` 的周期切换当前只允许 `周`，`日 / 月` 在模块头部保留禁用态，不再触发图表重算。
- `周` 视角横轴固定为日期，横轴控件不再承担当前可操作切换。
- 堆叠柱图仍只使用 `原始输入 / 缓存输入 / 输出` 三段，`推理 Token` 仍作为 `输出` 子集标记。
- 非零分层允许设置最小可见高度，用于解决真实比例差距太大时肉眼不可见的问题；tooltip 继续展示真实 token 数、占比和 API 等价成本。

## 13. 代码仓库页 v0.3.6 状态图

根据第一页最新实际截图壳层和第三页现有 Git 采集边界，生成以下代码仓库页状态图：

- 代码仓库页状态图：[repositories-page.png](./assets/design/v0.3.6/repositories-page.png)

当前观察：

- 第三页壳层已经和第一页实际截图统一，不再沿用旧稿中独立漂移的左栏和顶部工具栏。
- 顶部保留筛选与摘要节奏，但明确改成 `扫描目录 + 四张摘要卡`；其中只保留 `今日改动 / 近 7 日改动 / 本月改动 / 最近提交`。
- 中部左侧从“仓库排行”改成 `Git 仓库详情`，不再出现固定排名列、Token 排序列和风险等级伪字段。
- 中部右侧继续保留单仓库详情，但内容切换为 `工作区改动 / 最近提交 / 关联会话 / 最近 Codex 活动 / origin 地址` 这类当前真实可落地信息。
- 底部不再单独放“数据源根目录”和“GitHub 远程仓库信息”两张大卡，而是压缩为紧凑页脚说明，明确页面只基于本地 Git 与 Codex 本地会话。

## 14. 代码仓库页 v0.3.8 状态图

根据第三页新一轮反馈，参考 `dev-ledger` 为仓库页补回仓库级 Token / 成本信息，生成以下状态图：

- 代码仓库页状态图：[repositories-page.png](./assets/design/v0.3.8/repositories-page.png)

当前观察：

- 顶部四卡仍然只表达 `今日改动 / 近 7 日改动 / 本月改动 / 最近提交`，没有重新退回“投入 KPI 首屏化”。
- 左侧 `Git 仓库详情` 表新增 `Codex 投入` 列，主值显示累计 Token，次级一行显示 API 等价成本；这部分参考了 `dev-ledger` 的紧凑双层呈现，但没有把第三页做成账本页。
- 左表排序项补回 `按累计 Token / 按累计成本`，便于从工程活跃和 Codex 投入两个视角切换。
- 右侧 `已选仓库` 在保留活动指标的同时，补入 `累计 Token / 累计 API 等价成本 / 关联会话 / 最近 Codex 活动` 一排摘要，方便判断该仓库既活跃又是否消耗了更多 Codex 投入。
- 当前文档已明确：第三页的 Token / 成本只允许用现有合同里的累计口径，不伪造 `30 天成本`、`30 天 Token` 或 GitHub 云端成本字段。

## 15. 代码仓库页 v0.3.9 状态图

根据第三页进一步反馈，继续精修排序交互、右侧详情结构和最近提交权重，生成以下状态图：

- 代码仓库页状态图：[repositories-page.png](./assets/design/v0.3.9/repositories-page.png)

当前观察：

- 左侧 `Git 仓库详情` 不再需要额外一排排序按钮，排序语义已经收敛到表头本身；从视觉和交互上都更像真实工作台表格。
- 默认排序保持 `最近提交` 倒序，但“最近提交”不再占据顶部四卡，只保留在表头排序和当前仓库头部的一行新鲜度提示里。
- 顶部第四卡改成 `关联仓库`，比“全局最近一次提交时间”更稳定，也更贴近第三页回答“哪些仓库真正和 Codex 有关系”的目标。
- 右侧新增的投入信息不再是一整条生硬横排，而是独立成 `Codex 投入概览` 分区，用 `2 x 2` 指标矩阵承载 `累计 Token / 累计 API 等价成本 / 关联会话 / 最近 Codex 活动`。
- 最近提交区域只保留极少量记录或“最近一次提交 + 查看全部”，继续承担上下文补充作用，但不再抢占详情主空间。

## 16. 代码仓库页 v0.3.10 状态图

根据第三页最新图审，继续修正左侧边栏和顶部工具栏与第一页真实壳层的一致性，生成以下状态图：

- 代码仓库页状态图：[repositories-page.png](./assets/design/v0.3.10/repositories-page.png)

当前观察：

- 左侧边栏已经回收到第一页真实壳层语义，品牌区、主导航、激活态、底部设置区和版本文案都不再像另一套产品。
- 顶部工具栏也回到第一页同样的三列结构：左侧标题组、中部目录视角切换、右侧状态 / 刷新 / 快照时间。
- 搜索框和目录过滤已下移到内容区首行，不再挤进顶栏形成另一套按钮风格。
- 第三页当前的主要差异应只剩页面内容本身，而不再是应用壳层层面的视觉冲突。

## 17. 代码仓库页 v0.3.11 状态图

根据第三页继续收敛后的数据口径和壳层反馈，进一步去掉本地目录语义与重复设置入口，生成以下状态图：

- 代码仓库页状态图：[repositories-page.png](./assets/design/v0.3.11/repositories-page.png)

当前观察：

- 页面对象已经改成“定时同步 Git 仓库快照”，标题副文案不再强调本地工程目录。
- 顶栏中部被收回为空白节奏，不再强行放 `目录视角` 或 `扫描目录` 这类无用控件。
- 左侧主导航只保留 `总览 / Codex 账本 / 代码仓库`，`设置` 只在底部保留一处，不再重复出现。
- 内容区首行只保留仓库搜索和排序提示，左表和右侧详情继续承担主要信息量。
- 右侧详情不再展示 `工作区改动` 这类本地工作树状态，而是回到同步 Git 活动、Codex 投入和最近提交这三类当前更可信的信息。
