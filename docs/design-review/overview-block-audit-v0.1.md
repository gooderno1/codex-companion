# 总览页分区设计对照审计（v0.1）

- 创建时间：2026-06-04
- 设计基准：`docs/assets/design/v0.3.3/overview-natural-time.png`
- 实现基准：`v0.2.2-dev.26`
- 本轮目标：按区块对照设计图，避免只凭整页观感修改；同时修正额度卡真实数据口径。

## 1. 左侧导航区

设计要点：

- 产品图标与 `Codex Companion` 标题在同一视觉组内，标题顶线与图标顶线对齐。
- 非官方说明和本机读取状态放在标题下方，不压到标题上方。
- 当前页高亮为浅蓝底，图标为蓝色。
- `设置` 固定在底部。

本轮处理：

- 品牌区已改为图标 + 标题顶线对齐。
- `非官方 Codex 本机仪表盘` 与 `本机读取 · 无上传` 已移到标题下方。
- 当前继续使用本项目非官方自定义图标，不使用 OpenAI / Codex 官方资产。
- 当前需要继续收敛图标与标题间距、标题字号和说明文字权重，使品牌组更接近设计稿，而不是只做到结构同组。

`v0.2.2-dev.15` 截图差异：

- 当前品牌图标有圆角容器和较多内部留白，截图中视觉尺寸明显小于设计稿。
- 设计稿品牌标识是更大的蓝色几何符号，和 `Codex Companion` 标题组成强品牌组。

验收点：

- 1360x900 与 1080x720 下品牌区不换行错位。
- 设置入口始终可见。
- 品牌图标使用非官方自定义几何标识，视觉尺寸接近设计稿，不使用官方 OpenAI / Codex logo。
- 导航图标语义与设计稿一致：总览使用首页图标，账本使用文档账本图标，代码仓库使用代码方块图标。
- 当前页激活态由整行浅蓝底和蓝色图标表达，图标本身不额外套白色胶囊。

## 2. 顶部工具栏

设计要点：

- 页面标题、副标题、时间视角、状态、刷新、快照在同一顶栏内。
- `时间视角 / 自然时间 / 计费时间` 为单行文本切换，不使用外框。
- 顶栏底部只有轻描边。

本轮处理：

- 时间视角已改为单行 flex。
- 配色 token 改为深墨文字、浅蓝描边和更轻阴影。
- 当前继续要求时间视角切换内部不换行，`时间视角 / 自然时间 / 计费时间` 必须作为一个单行控件处理。

`v0.2.2-dev.14` 已处理：

- 已移除额外 `桌面总控台` 眉标。
- 页面标题、副标题已改为横向信息组。
- 右侧工作区已改为轻量白底，不再作为强浮层卡片。

验收点：

- 时间视角不能拆成上下两行。
- 右侧快照只出现一次。
- 不显示 `桌面总控台` 眉标。
- `总览` 与副标题同一行展示，顶部工具栏高度接近设计稿。
- 右侧工作区外层只保留轻描边，不使用强阴影和过大圆角。

## 3. 顶部四个指标卡

设计要点：

- 每张卡有大图标、标题、主数字、变化说明和状态标签。
- 大图标位于左侧，标题、主数字和变化说明在右侧形成同一信息组。
- 状态标签位于卡片底部或底部视觉区域，不占用右上角空间。
- 四卡字段固定为 `今日 Token / 本周 Token / 本月 Token / 今日代码改动`。
- 自然时间与计费时间只改变统计口径，不改变卡片结构。

差异与处理：

- 之前实现缺少指标卡大图标。本轮补充 `token / calendar / month / code` 四类自定义 SVG 图标。
- 当前图标采用内联 SVG，不新增外部资产依赖。
- 指标卡已改为“左侧图标 + 右侧内容 + 底部状态标签”。
- 当前实现的指标卡渐变底色和阴影仍比设计稿重，下一步应收敛为接近白底、浅蓝描边和极轻分层。

验收点：

- 四卡图标、标题和主数字层级清楚。
- 主数字只出现一次，不重复展示完整 token。
- 状态标签显示数据源状态，周期口径由顶部时间视角决定，不在每张卡右上重复。

## 4. 5H / 周额度卡

设计要点：

- 标题只显示 `5H 额度窗口` / `周额度窗口`，不再额外显示一行 `额度窗口`。
- 标题可带小图标。
- 圆环中心显示最新剩余百分比。
- 右侧明细展示当前额度周期内的 Token、API 等价成本、代码行数、会话数，每一行应有小图标辅助识别。
- 模型占比展示当前额度周期 Top 3。

本轮数据口径修正：

- `codexCollector` 现在从 `total_token_usage` 累计快照计算真实增量，避免重复累计。
- 无 token 增量但包含 `rate_limits` 的记录也会进入额度观测点。
- `dashboardCollector` 使用 `rate_limits.resets_at + window_minutes` 建立当前 5H / 周额度周期。
- 圆环使用最近一次 `rate_limits` 的真实剩余百分比。
- 右侧 Token、成本、会话数和模型占比使用当前额度周期内的真实增量。

验收点：

- 额度卡不显示重复 `额度窗口` 行。
- 圆环弧线和中心数字必须同时表达剩余百分比，不能中心显示剩余、弧线使用已用。
- 圆环下方展示当前额度周期起止时间，用于解释顶部自然时间卡和额度周期卡的统计差异。
- 底部证据文案只显示 `观测 N 次 · 重置 N 次`，不再写成 `额度观测 N 次`，避免重复字样占用宽度。
- 额度卡底部注记和观测证据优先保持一行展示，不能因为辅助说明额外抬高额度卡。
- 5H 与周额度的 Token / 成本不再只是自然时间窗口估算。
- 若周周期内发生额度重置，周期使用百分比按观测高点累计，剩余量仍以最新 `rate_limits` 为准。
- 额度卡底部展示当前周期 `rate_limits` 观测次数和重置次数，说明数据可信依据。

## 5. 项目概览区

设计要点：

- 项目概览位于底部，独立卡片，表格内部滚动。
- 页级为自然时间时展示 `日 / 周 / 月`。
- 页级为计费时间时展示 `5H / 周额度`。
- 排序交互应归入表头字段，不再使用 `按 Token / 按成本 / 按代码行 / 按最近活动` 右侧按钮。
- 二级周期切换紧跟 `项目概览` 标题，排序方向由表头箭头表达。

本轮处理：

- 当前布局已保持底部卡片完整露出。
- 计费时间下项目概览的 5H / 周额度边界跟随真实 `rate_limits` 周期。

`v0.2.2-dev.15` 已处理：

- 项目区主标题已改为 `项目概览`，不再使用 `当前周期内的项目消耗与工程活动`。

验收点：

- 页面整体不滚动。
- 只有表格容器允许内部滚动。
- 项目区主标题直接使用 `项目概览`，不再使用“项目消耗”叙述。
- 二级周期切换紧跟 `项目概览` 标题右侧，排序交互放入表头。
- 项目列展示项目小图标和项目名，图标用于扫读，不表示官方身份。
- 表格保留所有已发现本地项目；当前周期无活动项目显示 0 / `--`，不因无活动被过滤。

## 6. 页脚数据来源区

设计要点：

- 页脚应说明数据来源，而不是只显示价格来源。
- 数据来源说明包含 `Codex 本地 sessions`、`archived_sessions`、`本地 Git`、`不读取云端仓库信息`。
- 右侧或后方以 chip 展示当前 session 文件数、archived 文件数和仓库数量。

本轮差异：

- 当前实现只显示 `OpenAI API Pricing` 和 Codex rate card 定价来源，缺少数据来源 chips。

验收点：

- 页脚包含数据来源说明。
- 页脚展示 `session / archived / 仓库` 三个统计 chip。

## 7. 后续仍需单独确认的细节

- 若后续继续追求像素级接近设计图，需要为每个区块增加固定尺寸标注表。
- 当前图标为代码内 SVG，后续若要形成完整资产包，应迁移到统一 icon registry 或 SVG 文件目录。
- 账本页和代码仓库页应复用本审计格式，不再只做整页截图对照。

## 8. `v0.2.2-dev.14` 结构收敛记录

基于 `v0.2.2-dev.13` 截图与 `v0.3.3` 设计稿，本轮继续处理以下高置信差异：

- 顶部工具栏：已移除 `桌面总控台` 眉标，标题和副标题改为横向组合。
- 外层壳层：已去掉应用外边距和左右间隔，右侧主工作区改为轻量白底，不再使用强浮层阴影。
- 卡片视觉：顶部四卡、中部额度卡和项目概览已统一弱化渐变和阴影，保留浅蓝描边。
- 页脚：继续保留数据来源与定价来源，当前 `1080 x 720` 截图下仍完整可见。

仍需后续关注：

- 当前真实数据只有一个项目时，项目概览表比设计稿更空，这是数据状态差异，不应通过假数据填充。
- 若要进一步贴近设计稿，需要进入区块尺寸标注阶段，逐项约束左侧栏宽度、顶部栏高度、卡片高度、表格行高和底部页脚高度。

## 9. `v0.2.2-dev.15` 尺寸标注基线

标注来源：

- 设计图：`docs/assets/design/v0.3.3/overview-natural-time.png`，尺寸 `1536 x 1024`。
- 实现图：`local_dev_work/overview-1360x900-dev15.png`，尺寸 `1683 x 1079`。
- 标注方式：人工按区块边界估算，重点比较比例与层级，不把生成图像当成像素级规范。

| 区块 | 设计稿比例 / 观察 | `dev.15` 比例 / 观察 | 结论 |
| --- | --- | --- | --- |
| 左侧导航 | 约 17% 宽，白底固定栏 | 约 16% 宽，已接近 | 暂不调整 |
| 顶部工具栏 | 约 7% 高，标题和副标题同一行 | 约 7% 高，已接近 | 暂不调整 |
| 顶部四卡 | 约 16% 高，卡片留白更舒展 | 约 15% 高，已比 `dev.14` 略舒展 | 后续只做小幅微调 |
| 额度卡 | 约 37% 高，右侧明细与模型条完整 | 约 37% 高，但受真实模型名和数据行影响更满 | 不用假数据压缩，保持真实内容优先 |
| 项目概览 | 主标题就是 `项目概览` | 主标题已改为 `项目概览` | 已处理 |
| 页脚 | 数据来源低权重展示 | 数据来源和定价来源完整可见 | 暂不调整 |

## 10. `v0.2.2-dev.16` 品牌标识收敛

基于 `dev.15` 截图与设计稿，本轮处理左侧品牌区的图标形态：

- 图标从“圆角容器内的小型机器人/节点图”改为“更大的蓝色几何 C 形标识”。
- 标识仍为本项目自定义 SVG，不使用 OpenAI、Codex 或其他官方 logo。
- 图标占位从 `52px` 扩大到 `64px`，标题组跟随右移，使品牌区比例更接近设计稿。
- 其余导航项、设置入口和非官方说明不调整。

## 11. `v0.2.2-dev.17` 额度数据证据展示

基于 `dev-ledger` 的额度周期口径，本轮不改变额度数值计算，而是补齐前端可见证据：

- `PeriodMetric` 新增 `quotaEvidence`，暴露周期内 `rate_limits` 观测次数、重置次数、最高已用百分比和最近观测时间。
- `QuotaWindowCard` 底部注记新增 `观测 N 次 · 重置 N 次`。
- 圆环仍显示最近一次 `rate_limits` 余量；右侧仍显示当前额度周期 Token、成本、代码行数和会话数。
- 历史重置段明细不在总览页展开，后续更适合放到账本页或额度详情页。

## 12. `v0.2.2-dev.18` 额度注记文案收敛

`dev.17` 截图中额度卡底部已经展示了观测证据，但口径说明仍偏技术化：

- 注记从 `圆环为最近 rate_limits ...` 收敛为 `圆环=最近余量；右侧=当前周期累计。`
- 原始字段、重置段和计算公式保留在数据契约中，不在总览页展开。
- `观测 N 次 · 重置 N 次` 保留，用于说明数据可信依据。

## 13. `v0.2.2-dev.19` 项目概览保留全部本地项目

`dev.18` 截图中页脚显示已发现 5 个仓库，但项目概览在 `日 / 5H` 周期只显示当前有活动的 `codex-companion`：

- 原因是 `buildProjectOverviewPeriod` 过滤掉了 Token、代码、提交、会话均为 0 的项目。
- 这与“项目概览可滚动查看所有项目”的目标不一致。
- 本轮改为保留所有已发现本地项目；无活动项目显示 0 和 `--`，排序时自然排在有活动项目之后。

## 14. `v0.2.2-dev.19` 视觉细节二次收敛

基于用户对当前截图的细节反馈，本轮不再只调大色板方向，而是把视觉差异压到组件级规则：

- 左侧品牌组：缩小标题字号、增加图标与标题横向间距，标题顶线与图标顶线继续对齐，说明文字降低权重。
- 额度卡：标题仍只显示 `5H 额度窗口` / `周额度窗口` 一次；底部证据文案从 `额度观测 N 次` 改为 `观测 N 次`，并用单行布局降低占高。
- 顶部工具栏：时间视角控件强制单行，内部 tab 不允许换行。
- 配色：主标题 / 主数字、正文、辅助说明、表头和页脚分层取色，不把所有文字统一压成深墨色。
- 项目表：移除表头全大写，恢复设计稿中的正常标题大小写。

## 15. `v0.2.2-dev.20` 额度圆环与项目图标修正

继续对照 `v0.3.3` 设计稿和 `dev19-ui` 截图，发现两个可直接修正的差距：

- 额度圆环：当前中心数字显示 `剩余量`，但弧线使用 `usedPercent`，在 `99% / 100% 剩余` 时只显示很小一段蓝色弧线，语义与文档 `圆环=最近余量` 相反。
- 项目概览：设计稿项目列包含彩色项目小图标，当前只有文字，缺少可扫读锚点。

本轮要求：

- `QuotaWindowCard` 的 `--quota-progress` 改为使用 `remainingPercent`，圆环弧线与中心数字保持同一口径。
- 5H 额度窗口使用蓝色强调，周额度窗口使用绿色强调，贴近设计稿中两个额度窗口的区分。
- 项目列新增本项目自定义小图标，按项目名稳定映射颜色，不使用官方或第三方 logo。

## 16. `v0.2.2-dev.21` 左侧导航图标收敛

继续对照设计稿左侧栏，`dev20` 仍存在导航图标语义和视觉容器不一致：

- `总览` 仍使用网格图标，而设计稿是首页入口语义。
- `代码仓库` 仍使用立方体图标，而设计稿更接近代码方块 / 代码仓库入口。
- 导航图标外层有额外白色胶囊，激活态层级比设计稿更重。

本轮要求：

- `overview` 图标改为首页 SVG。
- `ledger` 保持文档账本语义但改得更像账本页入口。
- `repo` 图标改为代码方块 SVG。
- 导航图标去掉额外白色胶囊背景，激活态只保留整行浅蓝底和蓝色图标。

## 17. `v0.2.2-dev.22` 项目概览标题区层级修正

继续对照设计稿底部项目概览区，`dev21` 仍存在控制层级差异：

- 当前 `日 / 周 / 月` 和排序按钮都堆在卡片右上角。
- 设计稿中 `日 / 周 / 月` 是项目概览标题组的一部分，紧跟 `项目概览`；排序按钮才是右侧操作区。

本轮要求：

- 标题组改为 `项目概览 + 二级周期切换`。
- 右侧只保留排序切换。
- 该调整不改变项目表数据口径、排序逻辑和滚动容器。

## 18. `v0.2.2-dev.23` 额度周期范围可解释性

继续复核真实快照时，发现顶部自然日 Token 和当前 5H 额度窗口 Token 可能差距很大：

- 当前快照中自然日 Token 包含本地自然日 0 点以来的用量。
- 5H 额度卡只统计当前 Codex `primary` 窗口，窗口可能刚开始，因此 token / 成本显著小于自然日累计是合理结果。
- 如果总览页不显示额度周期起止，用户容易误判为额度数据错误。

本轮要求：

- 不改变 token 增量和额度周期计算逻辑。
- 在额度圆环下方展示当前周期范围，例如 `周期 03:25 - 08:25`。
- 保持底部注记短句化，不展开 `rate_limits` 字段名。

## 19. `v0.2.2-dev.24` Token / 额度数据审计

继续复核真实快照时，发现顶部自然日 Token 与当前 5H 额度窗口 Token 差距较大。为避免把合理周期差异误判为数据错误，本轮新增正式审计文档：

- 审计文档：`docs/data-audit/overview-token-quota-audit-v0.1.md`
- 审计方式：只读取 `token_count` 的数值字段和时间戳，不输出原始会话正文、用户输入、模型回复或路径明细。
- 审计结论：当前实现没有把今日首个 `total_token_usage` 累计快照直接计为自然日增量；自然日 Token 与 5H Token 的差异主要来自自然时间周期和当前额度周期不同。
- 本轮不改运行代码，只沉淀审计证据和后续诊断建议。

## 20. `v0.2.2-dev.25` 细节对齐与色阶收敛

基于用户对 `dev23` 运行截图的反馈，本轮聚焦四个可验证细节，不扩大页面结构：

- 左侧品牌区：当前标题视觉顶线略高于图标，品牌组不如设计稿稳定；本轮要求给文字组做少量基线补偿，保持图标和 `Codex Companion` 标题顶线接近。
- 额度卡：当前 `重置` 与 `周期` 分两行展示，虽然解释了额度边界，但额外占用一行高度；本轮要求合并为 `重置 HH:mm · 周期 HH:mm - HH:mm`，保留真实周期信息但不新增独立行高。
- 时间视角：`时间视角 / 自然时间 / 计费时间` 必须作为一个单行控件处理；桌面宽度下不能把该控件拆到第二行。
- 配色与字体色阶：继续以 `v0.3.3` 设计稿为基准，正文从偏深蓝灰收敛到 `#475569` 级别，副标题 / 表头 / 页脚保持 `#64748b`，证据和定价来源使用更浅弱辅助色；顶部四卡主数字不使用等宽字体，避免数字视觉比设计稿更硬。

验收方式：

- 更新 UI Contract 和组件映射后再改实现。
- 生成 `1360 x 900` 与 `1080 x 720` 真实 Electron 截图，人工核查品牌区、额度卡、顶部时间视角和文字色阶。
- 不改变额度数据、token 增量、项目概览和定价来源口径。

## 21. `v0.2.2-dev.26` 原目标完成度审计

本轮不修改运行代码，补齐“原目标是否已经可证明完成”的审计入口：

- 新增 `docs/goal-audit/overview-goal-completion-audit-v0.1.md`。
- 按原目标拆分为：分区设计对照、文档先行流程、图标资产、额度真实数据、`dev-ledger` 参考和最终可交付状态。
- 审计结论：总览页已达到当前文档化产品设计目标，可进入用户图审确认；但由于设计稿不是逐像素标注稿，且当前页面使用真实本机数据，不能声明像素级完全一致。
- 后续若用户继续指出差异，仍按本分区审计文档追加具体区块记录，再改实现并截图验证。

## 22. `v0.2.2-dev.27` 区块比例测量

为进一步减少主观判断，本轮新增视觉比例测量文档：

- 新增 `docs/design-review/overview-visual-measurement-v0.1.md`。
- 记录设计稿与 `dev25` 实现截图的图片尺寸、主要区块边界估算和比例差异。
- 当前结论：左侧边栏、顶部工具栏、额度卡和小窗口完整性已接近设计稿；顶部四卡高度略低于设计稿，项目概览高度受真实页脚和定价来源影响略低。
- 后续若继续精修，应优先针对顶部四卡高度和内部留白做局部调整，不应全局拉大间距。

## 23. `v0.2.2-dev.28` 顶部四卡舒展度修正

基于 `v0.2.2-dev.27` 的比例测量，本轮进入具体实现修正：

- 不改变四卡字段、数据口径、图标体系和状态标签语义。
- 仅调整 `metric-card` 的最小高度、内部行距和状态标签底部布局。
- 修正目标是让四卡更接近设计稿约 `16%` 的舒展比例，并避免状态标签贴近主数字。
- 验证必须重新生成 `1360 x 900` 与 `1080 x 720` 真实 Electron 截图，确认小窗口没有因为四卡变高而丢失页脚。

## 24. `v0.2.2-dev.29` 顶部四卡修正后复测

本轮不修改运行代码，补齐 `dev28` 截图后的比例复测：

- `docs/design-review/overview-visual-measurement-v0.1.md` 已记录 `dev28` 的区块比例。
- 顶部四卡从 `dev25` 的约 `14.6%` 提升到约 `15.3%`，接近设计稿约 `16.0%`。
- 额度卡高度仍约 `36.7%`，与设计稿约 `36.8%` 基本一致。
- `1080 x 720` 截图确认四卡增高后，额度卡、项目概览和页脚仍完整可见。
- 后续不应继续单纯增加四卡高度；若继续精修，应进入图标尺寸、标题基线、主数字字号和状态标签纵向位置等卡片内部标注。

## 25. `v0.2.2-dev.30` 顶部四卡内部权重微调

基于 `dev29` 的复测结论，本轮不再增加四卡高度，而是进入卡片内部标注和微调：

- 在 `docs/design-review/overview-visual-measurement-v0.1.md` 增加顶部四卡内部标注。
- 指标图标块从 `42px` 小幅增加到 `44px`，内部 SVG 从 `24px` 到 `25px`。
- 四卡标题权重从 `650` 提升到 `700`。
- 主数字字号从 `30px` 小幅增加到 `31px`。
- 不改变四卡字段、单位、状态标签、时间视角和任何数据口径。

## 26. `v0.2.2-dev.32` 左侧导航单行化

继续对照 `v0.3.3` 设计稿与 `dev31` 截图，左侧导航仍有一处明确差异：

- 设计稿左侧导航行只显示图标和主标签，信息非常克制。
- 当前实现为每个导航项额外显示二级说明，例如 `用量、额度、项目概览`、`口径、模型、会话归因`。
- 这些说明会让左侧导航比设计稿更高、更密，并且和主内容区的信息说明抢层级。

本轮要求：

- `总览 / Codex 账本 / 代码仓库 / 设置` 导航行只保留图标和主标签。
- 不改导航图标、不改路由、不改页面标题和副标题。
- 设置入口仍固定在左下方，但不显示 `后续页面设计中` 二级说明。
- 生成 `1360 x 900` 与 `1080 x 720` 真实 Electron 截图，确认左侧导航更接近设计稿且小窗口仍完整显示。

## 27. `v0.2.2-dev.33` 项目表格列线补齐

继续对照 `v0.3.3` 设计稿与 `dev32` 截图，项目概览区仍有一处明确差异：

- 设计稿项目表格有浅色横向行线和纵向列线，整体更像账本表格。
- 当前实现只有横向行线，列之间主要靠空白区分，`项目 / Token / API 等价成本 / 代码行数` 的列边界不如设计稿清晰。
- 项目概览是底部主要滚动区，列线能提升扫读效率，同时不影响数据口径。

本轮要求：

- 只给总览页项目概览表补充浅色纵向列线。
- 不影响会话表、账本页或仓库页的其他表格。
- 纵向线必须弱于文字和项目图标，不能形成重边框。
- 生成 `1360 x 900` 与 `1080 x 720` 真实 Electron 截图，确认列线生效且小窗口仍完整显示。

## 28. `v0.2.2-dev.34` 页脚归入项目概览卡

继续对照 `v0.3.3` 设计稿与 `dev33` 截图，底部数据来源仍有一处结构差异：

- 设计稿中数据来源说明、`session / archived / 仓库` chips 位于项目概览卡底部，视觉上属于同一张表格卡。
- 当前实现把 `footer-note` 放在项目概览卡之外，虽然内容正确，但底部信息与项目表格的归属关系弱于设计稿。
- 数据来源说明是可信度信息，不应删除；更合理的对齐方式是把它移动进项目概览卡底部。

本轮要求：

- 抽出 `FooterNote` 组件，继续复用同一份 `snapshot.sourceHealth` 与 `snapshot.pricingMeta` 数据。
- 总览页将 `FooterNote` 渲染在项目概览卡内部、表格下方。
- 账本页和代码仓库页继续在页面底部显示同一 `FooterNote`，不改变其他页面信息。
- 不改变 session / archived / 仓库数量和定价来源文本。
- 生成 `1360 x 900` 与 `1080 x 720` 真实 Electron 截图，确认总览页底部信息归属更接近设计稿且小窗口仍完整显示。

## 29. `v0.2.2-dev.35` 顶栏状态胶囊文字化

继续对照 `v0.3.3` 设计稿与 `dev34` 截图，顶部工具栏还有一处明确细节差异：

- 设计稿右上角状态胶囊只显示 `已观测` 文本。
- 当前实现为顶栏状态胶囊额外显示一个状态图标，导致状态胶囊更像按钮，宽度也比设计稿更大。
- 额度卡和顶部四卡的状态标签已经是文字-only，顶栏应保持同一克制表达。

本轮要求：

- 顶部工具栏状态胶囊移除状态图标，只保留状态文字。
- 不删除 `status` 图标资产，因为后续其他场景可能仍会使用。
- 不改变状态枚举、状态文案、刷新按钮、快照时间和数据源判断。
- 生成 `1360 x 900` 与 `1080 x 720` 真实 Electron 截图，确认顶栏更接近设计稿且时间视角仍保持单行。

## 30. `v0.2.2-dev.36` 项目概览默认周期对齐

继续对照 `v0.3.3` 自然时间设计稿与 `dev35` 截图，底部项目概览存在一个明确状态差异：

- 设计稿中 `项目概览` 标题右侧的二级周期默认高亮为 `周`。
- 当前实现首次进入总览页时默认高亮 `日`，导致项目表首屏更多展示当天稀疏活动，不如设计稿的周期概览稳定。
- 计费时间下同理，项目概览作为概览区应优先展示 `周额度`，而不是默认 `5H`。

本轮要求：

- 自然时间下项目概览默认选中 `周`。
- 计费时间下项目概览默认选中 `周额度`。
- 用户手动切换周期后，在当前总览页会话内继续保持用户选择。
- 不改变项目数据聚合、排序、字段、表格结构或项目保留规则。
- 生成 `1360 x 900` 与 `1080 x 720` 真实 Electron 截图，确认默认高亮和表格首屏更接近设计稿。

## 31. `v0.2.2-dev.41` 页面级验收截图版本绑定

继续复核总览页验收证据时发现，`npm run verify:overview` 已能串联设计 token 和额度快照校验，但仍静态依赖 `dev36` 截图：

- 代码和文档版本已经继续迭代到 `v0.2.2-dev.40` 之后。
- 如果验收脚本继续只检查 `overview-1360x900-dev36.png` 和 `overview-1080x720-dev36.png`，后续 UI 即使发生漂移，也可能沿用旧截图通过页面级验收。
- 这与“认真对照设计稿，争取全部改完”的目标不一致，因为页面级验收必须证明当前版本，而不是历史版本。

本轮要求：

- `verify:overview` 从 `package.json` 当前版本推导截图后缀。
- 新增 `npm run capture:overview`，按当前版本自动生成两张总览页真实 Electron 截图。
- 当前 `v0.2.2-dev.41` 必须检查 `overview-1360x900-dev41.png` 与 `overview-1080x720-dev41.png`。
- 目标审计和比例测量文档必须引用同一组当前版本截图。
- 本轮不修改 UI、额度数据、项目聚合或设计 token，只增强当前版本视觉证据的新鲜度。

## 32. `v0.2.2-dev.42` 顶部四卡新增语义修正

继续对照 `v0.3.3` 设计稿与 `dev41` 截图，顶部四卡仍有一处数据表达差异：

- 设计稿中四张指标卡的次级说明均展示相对上一周期的变化，例如 `较昨日 +12.4%` 或 `较昨日 -3.1%`。
- 当前实现已经为 Token 卡展示变化说明，但当上一周期为 `0` 且当前周期大于 `0` 时，`describeDelta` 会把它降级为 `数据待补齐`。
- 这不是数据缺失，而是百分比分母为 `0`，百分比不可定义；继续显示 `数据待补齐` 会误导用户以为 Git 或 Codex 数据没有接入。

本轮要求：

- `current === null` 或 `previous === null` 时继续显示 `数据待补齐`。
- `previous === 0 && current === 0` 时显示 `较上一周期 持平`。
- `previous === 0 && current > 0` 时显示 `较上一周期 新增`，并按正向变化使用绿色样式。
- 不伪造 `+100%` 或无限百分比，不改变 Token / 代码改动的实际统计值。
- 生成当前版本 `1360 x 900` 与 `1080 x 720` 真实 Electron 截图，确认顶部四卡次级说明不再把有数据场景误报为缺数据。

## 33. `v0.2.2-dev.43` 额度 Token 明细单位修正

继续对照 `v0.3.3` 设计稿与 `dev42` 截图，中部额度卡还有一处轻量但明确的数据展示差异：

- 设计稿右侧明细行 `Token 用量` 的值带 `tok` 后缀，例如 `21.5万 tok`。
- 当前实现只显示 `1242 万` 这类紧凑数字，虽然左侧标签已经写了 `Token 用量`，但扫读时不如设计稿明确。
- 顶部四卡和项目概览已经通过标题或表头表达 Token 语义，不需要额外加 `tok`，否则会让页面重复。

本轮要求：

- 仅在 `QuotaWindowCard` 右侧 `Token 用量` 明细值追加 `tok` 后缀。
- 保留既有 `万 / 亿` 自动单位规则。
- 不改变顶部四卡、项目概览表和模型占比的 Token 显示。
- 不改变任何 Token、额度、成本或 Git 统计口径。
- `capture:overview` 必须在生成当前版本截图前删除旧文件，并验证输出文件非空；如果 Electron 截图失败，不能误报当前版本截图已生成。

## 34. `v0.2.2-dev.44` 额度周期范围自适应显示

继续对照 `v0.3.3` 设计稿与 `dev43` 截图，中部额度卡圆环下方周期文案仍有一处可解释性问题：

- 5H 额度窗口显示 `周期 09:02 - 14:02` 这类同日时间范围时清晰。
- 周额度窗口因为起止时间通常相同，当前显示为 `周期 09:02 - 09:02`，扫读上容易被理解成零长度周期。
- 真实周期边界没有问题，问题只在前端展示格式。

本轮要求：

- 周期边界仍使用 `period.startAt / period.endAt`，不改 `dashboardCollector` 的周期计算。
- 同日短周期继续显示 `HH:mm - HH:mm`。
- 跨日但小于 24 小时的短周期显示 `MM/DD HH:mm - MM/DD HH:mm`。
- 周额度等多日周期显示 `MM/DD - MM/DD`，和重置时间合并在同一行。
- 不改变额度 Token、API 成本、代码行数、会话数、圆环余量、观测证据和模型占比。

## 35. `v0.2.2-dev.45` 目标审计运行态证据刷新

继续复核目标完成度审计时发现，审计文档中的 5H / 周额度运行态摘要仍停留在旧快照：

- 当前 `snapshot.json` 已显示 5H / 周额度周期为 `2026-06-04T01:02:51Z` 到对应结束时间。
- 当前周期 Token、API 等价成本、代码行数、会话数、观测次数和 sourceHealth 数量均已变化。
- 如果目标审计继续保留旧数值，会削弱“额度数据已接入真实数据”的证据可信度。

本轮要求：

- 只读取当前运行态聚合字段，不输出原始会话正文或用户输入。
- 刷新目标审计中的 5H / 周额度周期、Token、成本、代码行数、会话数、观测证据、圆环余量和价值折算摘要。
- 刷新页脚数据来源摘要中的 session / archived / 仓库数量。
- 重新生成当前版本 `1360 x 900` 与 `1080 x 720` 截图，确保页面级验收仍绑定当前版本。
- 不改变 UI、额度计算、Token 增量、Git 聚合或图标资产。

## 36. `v0.2.2-dev.46` 页面级验收覆盖原目标关键不变量

继续复核 `npm run verify:overview` 的覆盖范围时发现，当前脚本已经检查设计图、文档、截图和基础组件标记，但对原目标中的若干关键约束仍是间接证据：

- 挂件当前必须隐藏，不能因为后续改动重新打开。
- 额度数据必须来自 Codex `rate_limits`、连续 `total_token_usage` 增量和当前额度周期，而不是静态估算。
- 额度价值折算必须使用周期累计 `quotaEvidence.usedPercent`，不能回退到最近一次原始百分比。
- 额度卡周期文案和 `tok` 后缀等近期设计细节不能回退。
- 设计图标必须继续使用本项目自定义组件，不引入官方或第三方品牌图标。

本轮要求：

- `verify:overview` 增加 `src/main/index.ts`、`codexCollector.ts`、`dashboardCollector.ts` 和 `docs/data-contract-v0.2.md` 的关键内容检查。
- 校验 `WIDGET_DISABLED`、`rate_limits`、`total_token_usage`、`quotaEvidence`、`estimatedValueBasisUsedPercent`、`formatQuotaPeriodRange` 和 `tok` 等关键标记。
- 继续串联 `verify:design` 和 `verify:quota`，避免只做静态字符串检查。
- 本轮不改变 UI、采集逻辑、额度算法或图标实现，只增强回归保护。

## 37. `v0.2.2-dev.47` 总览页视觉细节收敛

继续对照 `v0.3.3` 设计稿与 `dev46` 截图，当前页面主体结构已经接近，但仍有四个细节偏差：

- 左侧品牌图标和 `Codex Companion` 标题虽然同组展示，但图标视觉盒偏高，标题组存在轻微下坠感。
- 额度卡底部说明仍带具体周期名称，例如 `当前 5H 周期累计` / `当前周额度周期累计`，与标题里的 `5H 额度窗口` / `周额度窗口` 形成重复阅读负担。
- 顶部 `时间视角 / 自然时间 / 计费时间` 必须始终作为一行文字控件，不允许在较窄桌面窗口拆成上下两行。
- 当前配色方向正确，但文字色阶、边框蓝色透明度、导航激活底色和卡片阴影仍比设计稿更重或更蓝。

本轮要求：

- `ui-contract` 先补充品牌基线、额度说明短句、时间视角 inline-flex 和新色阶约束。
- `design-tokens.ts` 与 `verify-design-tokens.mjs` 同步更新，颜色变化必须可执行校验。
- 品牌图标视觉盒收敛到约 `56px`，标题组不再额外下移。
- 额度卡底部注记统一为 `圆环=最近余量；右侧=当前周期累计。`，不重复 `5H`、`周额度` 或 `额度窗口`。
- 顶部时间视角容器增加单行保护，优先压缩两侧间距，不把切换项拆行。
- 不改变任何 Token、额度、成本、Git 统计口径、项目排序或图标语义。

## 38. `v0.2.2-dev.48` 顶栏时间视角位置修正

继续对照 `v0.3.3` 设计稿与 `dev47` 截图，顶部工具栏仍有一处壳层位置差异：

- 设计稿中 `时间视角 / 自然时间 / 计费时间` 位于顶部工具栏中段，作为页面控制区。
- `dev47` 中该控件虽然保持单行，但因为 `.topbar` 使用 `space-between` 且标题组占用弹性空间，时间视角被推到右侧操作区附近。
- 这会削弱“左侧页面识别、中部页面控制、右侧数据操作”的壳层结构，也会影响后续账本页和仓库页复用顶栏。

本轮要求：

- 先在 UI Contract 中补充顶栏中部位置规则。
- 顶栏布局改为“标题组固定/可收缩宽度 + 中部视角控件 + 右侧操作区 `margin-left: auto`”。
- 时间视角继续保持同一行文本切换，不增加外框，不改变自然时间 / 计费时间行为。
- 右侧状态、刷新、快照仍贴右；快照只出现一次。
- 不改变顶部四卡、额度卡、项目概览、设计 token 或任何数据口径。

## 39. `v0.2.2-dev.49` 顶部四卡逐卡状态修正

继续复核 `自然时间 / 计费时间` 切换口径时发现，顶部四卡状态仍存在一个数据表达问题：

- `MetricCard` 当前统一接收 `snapshot.sourceHealth.sourceStatus`。
- 当计费时间下 `本月 Token` 的 `billingMonth` 尚未稳定观测时，卡片主值显示 `未观测`，说明显示 `计费月数据待补齐`，但状态标签仍可能显示全局 `已观测`。
- 这会让用户误以为计费月额度已经被观测到，与 UI Contract 中“当前数据源尚不可稳定观测时标记为未观测或待补齐”的口径冲突。

本轮要求：

- 顶部四卡数据结构增加逐卡 `sourceStatus`。
- 自然时间四卡和计费时间中已有稳定数据的卡片继续沿用全局 `sourceHealth.sourceStatus`。
- 计费时间下 `billingMonth` 缺失时，`本月 Token` 卡片使用 `unobserved`。
- `verify:overview` 增加静态不变量检查，避免后续回退为全局状态。
- 不改变 Token、额度、成本、Git 聚合、卡片字段和默认自然时间截图布局。

## 40. `v0.2.2-dev.50` 自然/计费双状态截图验收

继续复核页面级证据时发现，当前 `capture:overview` 只生成默认自然时间截图：

- 设计稿和 UI Contract 都包含 `自然时间 / 计费时间` 两种状态。
- `v0.2.2-dev.49` 已修正计费时间下单卡状态，但真实 Electron 截图仍无法证明计费时间 UI 实际可达。
- 如果只保留自然时间截图，后续可能在计费时间状态出现布局、状态标签或项目周期切换回退而不被页面级验收发现。

本轮要求：

- 主进程截图入口支持 `CODEX_COMPANION_OVERVIEW_MODE=billing`。
- 渲染层从 hash 查询参数读取初始 `overviewMode`，不影响普通用户默认进入自然时间。
- `capture:overview` 生成自然时间和计费时间各两张截图，共四张。
- `verify:overview` 要求四张当前版本截图均存在且非空。
- 不改变任何统计口径、额度计算、默认页面状态或用户手动切换行为。

## 41. `v0.2.2-dev.51` 品牌、额度说明、时间视角和色阶精修

基于用户对 `dev50` 截图的细节反馈，本轮继续做区块级收敛，不改变任何数据口径：

- 左侧品牌区：`BrandMark` 迁移到 `src/renderer/icons.tsx`，品牌图标视觉盒相对标题上移少量像素，让图标视觉顶线与 `Codex Companion` 标题更接近设计稿。
- 额度卡：不再把 `windowData.note` 作为独立底部说明行渲染；`观测 N 次 · 重置 N 次` 并入 `Top 3 模型占比` 标题行右侧，避免在标题已经写明 `5H / 周额度窗口` 后再用辅助说明多占一行。
- 顶栏时间视角：新增 `topbar-mode-switch` 单一 inline-flex 容器，确保 `时间视角 / 自然时间 / 计费时间` 是同一行控件，而不是由多个可换行节点拼出来。
- 配色细节：设计 token 从 `#101828 / #344054 / #667085 / #8a97a8` 调整为 `#111827 / #334155 / #64748b / #94a3b8`，并微调背景、主蓝、状态色和导航激活底色，使字体色阶与设计稿更接近。

验收边界：

- 不改变顶部四卡字段、额度真实数据、项目概览、页脚来源和自然/计费切换逻辑。
- `1360 x 900` 与 `1080 x 720` 的自然/计费四张截图必须重新生成到 `dev51`。
- 如果后续继续指出颜色差异，应继续先补 token 文档，再改实现，不允许只做零散 CSS 覆盖。

## 42. `v0.2.2-dev.52` 实时刷新、四卡状态和顶栏居中修正

基于用户对 `dev51` 截图和运行态的反馈，本轮聚焦可见交互与顶部区域细节，不改变统计口径：

- 数据刷新：当前渲染端只在启动时读取快照，主进程后台采集完成后没有广播，应用运行期间也没有周期刷新，因此页面看起来不是实时更新。
- 顶部四卡：`数据过期` 等状态标签位于独立底行，既占高度，也破坏卡片内部重心。
- 顶部四卡：图标尺寸固定为 `44px`，标准窗口下相对主数字偏小；标题、主数字和变化说明作为右侧内容组左对齐，和设计稿中更居中的视觉重心不一致。
- 顶栏时间视角：虽然已保持单行，但当前 `flex` 分配仍可能让中部控件偏左，未形成真正的顶栏几何居中。

本轮要求：

- `ui-contract` 先补充主进程周期刷新、后台刷新广播和渲染端订阅规则。
- 主进程新增 `dashboard:updated` 广播，启动后台采集、手动刷新和 `60s` 周期刷新完成后都推送最新快照。
- 渲染端新增 `onDashboardUpdated` 订阅，自动刷新时静默替换快照，不触发首屏加载遮罩。
- 顶部四卡状态标签固定到右上角，不再单独占一行。
- 顶部四卡内容区整体居中，文字组居中对齐，图标使用 `clamp()` 自适应尺寸。
- 顶栏桌面态改为左 / 中 / 右三列布局，时间视角在顶栏可用区域内居中。

验收边界：

- 不改变顶部四卡字段、单位、变化说明、逐卡状态口径、额度数据、项目概览和页脚。
- `1360 x 900` 与 `1080 x 720` 的自然/计费四张截图必须重新生成到 `dev52`。
- 自动刷新不应制造明显闪烁；只有手动点击刷新才显示 `刷新中`。

## 43. `v0.2.2-dev.53` 额度剩余量与顶部图标细节修正

基于用户对 `dev52` 的反馈，本轮发现额度圆环存在一个更底层的口径问题：

- 之前圆环使用最近一次原始 `rate_limits.used_percent` 推导余量。
- 当前周额度周期内，最近一次原始百分比可能回落到低水位，但周期累计 `quotaEvidence.usedPercent` 已经消耗到 `6%`。
- 这会让周额度圆环显示 `100%` 剩余，和当前周期累计消耗事实不一致。
- `dev-ledger` 中前几周计费周记录已经证明应以周期累计余量作为页面可见口径，例如当前周 `usedPercent=6%` 时余量应显示 `94%`。

本轮要求：

- `LimitWindow.usedPercent / remainingPercent` 改为页面显示字段，优先使用 `PeriodMetric.quotaEvidence.usedPercent / remainingPercent`。
- 最近原始 `rate_limits` 只作为缺少周期证据时的降级来源。
- Codex session 采集窗口扩展到近 `60` 天，使前几周计费周可从本机原始 session 重建；不硬编码依赖 `dev-ledger` 路径。
- `verify:quota` 改为断言圆环余量等于当前周期 `quotaEvidence.remainingPercent`。
- 顶部四卡图标最小尺寸提升到 `52px`，SVG 占比提升到 `60%`，避免标准窗口下仍显得偏小。
- 额度圆环内部小字 `剩余量` 固定在百分比上方，百分比处于圆心视觉位置。

验收边界：

- 不改变顶部四卡字段、时间视角、项目概览和真实 token 增量算法。
- 不把 `dev-ledger` 作为运行时数据源；它只用于核对历史周期口径。
- `1360 x 900` 与 `1080 x 720` 的自然/计费四张截图必须重新生成到 `dev53`。

## 44. `v0.2.2-dev.54` 主额度池和昨日代码口径修正

基于用户继续指出的两处数据问题，本轮确认问题发生在采集聚合层：

- 额度：当前 Codex 原始会话里同时存在主额度池 `limit_id=codex` 和模型池 `limit_id=codex_bengalfox`；之前按最新观测时间选快照，会让模型池覆盖主额度，导致 5H 剩余量显示错误。
- 代码：`今日代码改动` 的昨日比较只构造了昨日 token 周期，没有注入昨日自然日 Git 代码活动，因此即使昨日 Git 有改动，也会被当成 `0`。

本轮要求：

- `codexCollector` 保留 `limitId / limitName`，可见额度优先选择 `limit_id=codex`。
- `dashboardCollector` 只把同一额度池样本纳入当前额度周期证据。
- `gitCollector` 与数据契约补充 `activity.yesterday`，顶部四卡使用昨日自然日 Git 行数计算 `较昨日`。
- `verify:overview` 增加主额度池选择和昨日代码聚合的静态回归检查。

验收边界：

- 不改变总览页布局、配色、图标和顶部四卡固定字段。
- 不把 `dev-ledger` 作为运行时数据源；它只用于核对“昨日有数据”这一事实。
- `1360 x 900` 与 `1080 x 720` 的自然/计费四张截图必须重新生成到 `dev54`。

## 45. `v0.2.2-dev.57` 顶部四卡图标与状态标签微调

基于用户对顶部四张指标卡的视觉反馈，本轮只处理卡片内部关系，不改变统计口径：

- 顶栏已经展示全局 `已观测`，四张指标卡继续重复正常态状态标签会制造噪声。
- 当前图标、字体和数字风格已经可用，但图标与右侧三行文字仍像两个独立中心；更合理的关系是图标视觉高度接近三行文字总高度，形成一个完整指标组。

本轮要求：

- `MetricCard` 保留逐卡 `sourceStatus` 数据，但当状态为 `observed` 时不渲染单卡状态标签。
- 当单卡为 `pending / unobserved / stale` 时，仍在右上角显示异常态状态标签，尤其保证计费月未观测不会被隐藏。
- 顶部四卡图标盒调整到约 `64px - 72px`，接近右侧标题、主数字和说明三行文字的总高度。
- 图标与文字组间距收紧到约 `14px - 18px`，避免图标和文字分散。

验收边界：

- 不改变顶部四卡字段、主数字、变化说明、单位、图标语义和数据口径。
- 不全局拉大卡片间距或页面高度，避免重新造成小窗口溢出。
- `1360 x 900` 与 `1080 x 720` 的自然/计费四张截图必须重新生成到 `dev57`。

## 46. `v0.2.2-dev.61` 计费月 Token 默认口径修正

基于用户从套餐信息确认的月度计费起始规则，本轮修正计费时间下顶部 `本月 Token` 的数据状态：

- 当前套餐计费月从每月 `1` 日开始，因此默认计费月 Token 与自然月 Token 一致。
- Codex 原始 `rate_limits` 仍未暴露月额度窗口，因此 `可观测月额度` 不能因此变成已观测。
- 后续设置页应提供 `billingMonthStartDay`，让用户选择每月第几天作为计费月起始日。

本轮要求：

- `AppPreferences` 增加 `billingMonthStartDay`，默认 `1`。
- `dashboardCollector` 使用 `startOfBillingMonth(now, billingMonthStartDay)` 构建 `overview.windowPeriods.billingMonth`。
- 计费时间下顶部 `本月 Token` 使用该计费月周期，不再显示 `未观测`。
- `observableMonthWindow` 继续保持月额度未观测，避免把计费月 Token 当成月额度。

验收边界：

- 不改变自然时间顶部四卡字段和数值口径。
- 不改变 5H / 周额度真实 `rate_limits` 口径。
- `1360 x 900` 与 `1080 x 720` 的自然/计费四张截图必须重新生成到 `dev61`。

## 47. `v0.2.2-dev.68` 项目概览表头排序修正

基于用户对底部项目概览的交互反馈，本轮只调整排序入口，不改变项目聚合口径：

- 右上角 chip 排序按钮会额外占用工具区，并且和表格字段距离较远。
- 更自然的交互是直接点击表头字段排序，让排序能力和数据列绑定。
- 项目概览默认应优先展示最新项目活动，而不是默认 Token 降序。

本轮要求：

- 移除 `按 Token / 按成本 / 按代码行 / 按最近活动` 右侧排序按钮。
- `项目 / Token / API 等价成本 / 代码行数 / 提交 / 会话 / 最近活动` 表头都可点击排序。
- 默认排序为 `最近活动` 倒序。
- 点击非当前排序表头时，第一次按该字段正序排序；再次点击同一表头时切换为倒序。
- 活动排序列用轻量蓝色状态和方向箭头提示，不新增表格外排序控件。

验收边界：

- 不改变项目概览的周期切换、表格字段、项目保留规则和数据来源。
- 不改变顶部四卡、额度卡和页脚布局。
- `1360 x 900` 与 `1080 x 720` 的自然/计费四张截图必须重新生成到 `dev68`。
